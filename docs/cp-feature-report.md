# تقرير ميزة صلاحيات CP ولوحة التحكم

**الفرع:** `feat/cp-permissions-admin` (متفرّع من `main` عند `07143fc`)
**التاريخ:** 2026-09-23
**المرجع:** `docs/cp-system-analysis.md` (التحليل والتصميم الكامل)

> **CP هنا = العلاقة (Couple Pairing):** اتنين مرتبطين بهدية CP اتقبلت. مالهاش علاقة بإعداد «قوة الحساب» القديم `cp_per_coin` / `cpPoints` اللي في صفحة الإعدادات.

---

## 1. الملخص

- **فتح CP بقى له سياسة:** مجاني أو برسوم، للكل أو لمستخدم معيّن. السيرفر هو اللي بيقرر وهو اللي بيخصم.
- **التطبيق بيعرض الرسوم قبل التأكيد:** مفيش خصم غير بعد «موافق»، والخصم ذري. لو الرصيد مش كافي بتظهر رسالة بالناقص ومفيش أي خصم.
- **مستوى الـ CP وقيمته من السيرفر.** «المستخدم الظاهر» كمان بقى متخزّن على السيرفر بدل الجهاز.
- **لوحة التحكم فيها تبويبين جداد:** «CP — العلاقة» و«الخلفيات».
- **كل عملية أدمن بتتسجّل** في `admin_audit_logs`: مين عمل إيه، لمين، القيمة قبل وبعد، الوقت، والـ IP.
- **الافتراضيات مش بتغيّر حاجة لأي مستخدم حالي** لحد ما المسؤول يقرر بنفسه.

### الكوميتات

| الكوميت | المحتوى |
|---|---|
| `8bc2901` | تحليل النظام الحالي والتصميم (`docs/cp-system-analysis.md`) |
| `166228a` | الـ backend: الـ migration، الـ services، الـ routes، اختبارات سياسة الفتح |
| `fad8146` | لوحة التحكم: تبويب CP |
| `e744dfc` | لوحة التحكم: تبويب الخلفيات |
| `d9ddecd` | تطبيق Flutter — **مش متجرّب (مفيش Flutter SDK)** |
| `373db71` | اختبارات الـ backend الإضافية |

الإجمالي: 27 ملف، +4426 / −75 سطر. **كله local ولسه ما اتعملوش push.**

---

## 2. اللي اتبنى — ملف بملف

### 2.1 الـ Backend

| الملف | إيه اللي فيه |
|---|---|
| `prisma/migrations/20260922000000_cp_permissions_admin/migration.sql` | الجداول والأعمدة الجديدة والـ backfill (التفاصيل في القسم 3) |
| `prisma/schema.prisma` | نفس التغييرات في Prisma: `CpUnlockGrant`، `CpUnlockGrantHistory`، `CpUnlock`، `AdminAuditLog`، و`CpPair.cpValue/levelOverride`، و`User.cpFeaturedPartnerId` |
| `src/services/cpUnlock.service.ts` | سياسة الفتح، وفيه الدوال دي: `resolveCpUnlockPolicy` (منحة المستخدم بتغلب سياسة النظام)، `getCpUnlockStatus` (الرسوم والرصيد والناقص)، `confirmCpUnlock` (خصم ذري + `Transaction(CP_UNLOCK_FEE)` + سجل الفتح، ولو اتضغط مرتين مفيش خصم تاني)، `assertCpUnlocked` (بوابة `POST /cp/requests`)، و`computeCpLevel` (حساب المستوى) |
| `src/services/cpAdmin.service.ts` | كل عمليات اللوحة: السياسة، المنح والسحب (مع سجل تاريخ)، فتح وقفل CP لمستخدم، الأزواج، هدايا CP، سجل العمليات، الخلفيات (إضافة/تعديل/حذف/منح/سحب/قائمة المستخدم)، و`canUseProfileBackground` |
| `src/services/adminAudit.service.ts` | `recordAdminAudit()`: بيكتب سجل المراجعة، ولو الكتابة فشلت العملية نفسها ما بتقعش |
| `src/routes/adminCp.routes.ts` | مسارات `/api/v1/admin-dashboard/cp/*` و`/backgrounds/*`، وكلها ورا `authenticate` + `requireAdminDashboard` (صلاحية الأدمن بتتشيك من السيرفر) |
| `src/routes/adminDashboard.routes.ts` | ربط المسارين الجداد |
| `src/routes/cp.routes.ts` | المسارات الجديدة: `GET /cp/unlock/status`، `POST /cp/unlock/confirm`، `PATCH /cp/featured`. و`POST /cp/requests` بيرجّع **403 `CP_LOCKED`** ومعاه الرسوم لو المستخدم لسه ما فتحش |
| `src/services/cp.service.ts` | قائمة الشركاء بترجع `level` و`levelName` و`cpValue` و`days` و`featured` من السيرفر. وفيه `setFeaturedPartner`. وهدية CP لشريك موجود بتتبعت على طول وبترفع المستوى (القسم 8) |
| `src/services/cpGift.service.ts` (جديد) | قواعد «هدايا CP» و«المستخدم الظاهر» (القسم 8) |
| `src/gifts/giftService.ts` | بقى **ما بيرفعش** قيمة CP. الرفع بقى من هدايا CP بس، عن طريق `cp.service` |
| `src/controllers/user.controller.ts` | `PUT /users/me` بيرفض يحط خلفية من المتجر المستخدم مش مالكها: `403 BACKGROUND_NOT_OWNED` |
| `src/controllers/adminProduct.controller.ts` | تصدير `publicBaseUrl` و`buildPosterUrl` عشان رفع الخلفيات يستخدمهم. نفس السلوك |
| `src/controllers/settings.controller.ts` | القيم الافتراضية لمفاتيح الإعدادات الجديدة |
| `package.json` | سكربت `npm test` |

### 2.2 لوحة التحكم (`backend/public/admin-dashboard.html` + `app.js`)

vanilla JS بنفس أسلوب الصفحة، ومن غير أي framework جديد. كل النداءات بتروح على المسارات اللي في `adminCp.routes.ts` بس.

**تبويب «CP — العلاقة»:**
1. **سياسة الفتح:** مجاني أو برسوم وعدد الكوينز، وقيمة رفع المستوى، وأقصى مستوى، وأسماء المستويات. الحفظ بيطلب تأكيد.
2. **صلاحية مستخدم معيّن:** «فتح CP مجانًا» أو «فتح CP برسوم» بالـ Display ID مع ملاحظة، وجدول بالصلاحيات الحالية فيه عرض وسحب.
3. **بيانات CP لمستخدم:**
   - الرصيد، وحالة الفتح ومصدره، والسياسة اللي عليه، والمستخدم الظاهر، والدعوات المعلّقة.
   - أزرار فتح وقفل CP له.
   - جدول الأزواج: تعديل قيمة CP، وتثبيت المستوى (فاضي = تلقائي)، واختيار المستخدم الظاهر، وفك الارتباط.
   - **سجل هدايا CP:** كل هدية رفعت المستوى، من مين لمين، و+CP، والقيمة بعدها.
4. **هدايا CP:** السعر، و**«رفع مستوى CP»** لكل هدية (فاضي = نفس السعر)، والاسم بالعربي، والترتيب، والتفعيل.
5. **سجل العمليات:** فلتر بالمستخدم وبنوع العملية (CP / خلفيات / الكل)، و«تحميل المزيد».

**تبويب «الخلفيات»:**
1. **إضافة:** رفع صورة أو فيديو mp4/webm، مع الاسم، ومجانية أو السعر، والمدة (فاضي = أبدي)، و«خاصة (خارج المتجر)».
2. **كل الخلفيات:** معاينة، وتعديل مباشر في الجدول، وعدد الملّاك، وحذف بتأكيد بيوضّح كام مستخدم هيفقدها.
3. **منح / سحب لمستخدم:** مع سبب السحب.
4. **خلفيات مستخدم:** اللي بيملكه، وتاريخ الانتهاء، والمركّبة حاليًا، وسحب.
5. **زرار «سجل العمليات»:** بيفتح سجل تبويب CP متفلتر على عمليات الخلفيات.

### 2.3 تطبيق Flutter (`app/lib`)

| الملف | التغيير |
|---|---|
| `widgets/cp_unlock_gate.dart` (جديد) | قبل إرسال هدية CP: `GET /cp/unlock/status`. لو فيه رسوم بيظهر dialog بالرسوم والرصيد و«موافق — ادفع X». بعد الموافقة `POST /cp/unlock/confirm`. لو الرصيد مش كافي: «رصيدك لا يكفي لفتح الـ CP — ينقصك N كوينز» ومفيش خصم. لو مجاني: مفيش dialog أصلًا |
| `gifts/widgets/gift_picker_sheet.dart` | إرسال CP بيعدّي على البوابة الأول. لو السيرفر رجّع `CP_LOCKED` البوابة بتشتغل تاني مرة واحدة. الرصيد بيتحدّث من رقم السيرفر |
| `repositories/cp_repository.dart` | `unlockStatus()` و`confirmUnlock()` و`setFeatured()`. و`CpException` بقى شايل `feeCoins` و`balance` و`shortfall`. و`CpPartner` فيه `cpLevel` و`cpLevelName` و`cpValue` و`cpDays` و`featured` من السيرفر |
| `widgets/cp_relationship.dart` | الكارت بيعرض المستوى والاسم والأيام من السيرفر. سلّم الأيام القديم بقى fallback بس |
| `screens/cp_list_screen.dart` | النجمة بتحفظ المستخدم الظاهر على السيرفر (ولو فشلت بترجع زي ما كانت). وكل صف بيعرض مستوى الزوج |
| `screens/profile_screen.dart` | كارت العلاقة بيعمل reload بعد الرجوع من القائمة عشان يبان التغيير |
| `test/cp_unlock_models_test.dart` (جديد) | اختبارات لقراءة البيانات واختيار المستخدم الظاهر |

### 2.4 الاختبارات (`backend/src/cp/__tests__/`)

بتشتغل بـ `node:test` على fake Prisma في الذاكرة (`fakeDb.ts`)، من غير داتابيز: **70 اختبار، كلهم ناجحين** (منهم 18 في `cpGift.test.ts`، شوف القسم 8).

- **`cpUnlock.test.ts` (19):**
  - السياسة: الافتراضي مجاني، والمنحة بتغلب النظام، ومنحة مستخدم ما بتأثرش على غيره.
  - عرض الرسوم من غير خصم.
  - الخصم الذري: مرة واحدة، والضغط المزدوج ما بيخصمش تاني، والرصيد غير الكافي بيرجّع 402 بالناقص ومفيش أي خصم.
  - بوابة `POST /cp/requests` في حالاتها التلاتة: مجاني، برسوم، ومقفول.
  - حساب المستوى.
- **`cpAdmin.test.ts` (33):**
  - المنح والسحب مع صفوف التاريخ والـ audit (قبل/بعد/IP).
  - فتح وقفل من الأدمن من غير ما يتحرك أي كوين.
  - السياسة والأزواج.
  - فشل كتابة الـ audit ما بيوقعش العملية.
  - الخلفيات: الإضافة والتعديل والمنح (المنح المتكرر للأبدي بيترفض، وللمؤقت بيمد المدة) والسحب والحذف، **من غير ما ملكية أي حد تاني تتأثر**.
  - التطبيق ما يقدرش يحط خلفية مش ملكه.
  - مسارات `/cp` المتاحة للتطبيق ثابتة، ومفيش ولا واحد منها بيقرا كوينز أو قيمة CP أو ملكية من الطلب.

---

## 3. الـ Migration وحفظ البيانات الحالية

`20260922000000_cp_permissions_admin` **إضافي بالكامل (additive)**. كل الأوامر فيه `IF NOT EXISTS` أو `ON CONFLICT DO NOTHING`، وممكن يتشغّل أكتر من مرة من غير مشاكل.

**اللي بيتضاف:**
- `users.cpFeaturedPartnerId`
- `cp_pairs.cpValue` (افتراضي 0) و`cp_pairs.levelOverride` و`cp_pairs.updatedAt`
- جداول: `cp_unlock_grants` (منحة واحدة لكل مستخدم)، `cp_unlock_grant_history` (ما بيتمسحش أبدًا)، `cp_unlocks` (واحد لكل مستخدم)، `admin_audit_logs`

**الـ backfill (حفظ اللي موجود):**
1. **`cpValue`:** كل زوج بياخد قيمة الهدية اللي عملت الارتباط، من آخر طلب CP مقبول بين الاتنين. الزوج اللي مالوش طلب مطابق بيفضل 0، ومع الإعدادات الافتراضية ده ما بيغيّرش حاجة في العرض.
2. **`cp_unlocks`:** أي حد عنده زوج CP، أو بعت طلب CP واتقبل قبل كده، بيتسجّل إنه **فاتح CP مجانًا** (`source = 'legacy'`). لو المسؤول حوّل السياسة لـ«برسوم» بعدين، المستخدمين دول مش هيتقفل عليهم.
3. **الإعدادات:** `cp_unlock_mode = free`، و`cp_unlock_fee_coins = 0`، و`cp_level_step_coins = 0`، و`cp_level_max = 5`. بتتضاف **بس لو مش موجودة**.

**اللي ما بيتلمسش:** مفيش أي صف في `users` أو `user_items` (ملكية الخلفيات) أو `cp_pairs` بيتمسح، ومفيش عمود موجود بيتكتب عليه. **مستوى كل زوج حالي فاضل زي ما هو بالظبط**، لأن `cp_level_step_coins = 0` معناها استخدام سلّم الأيام القديم (0/7/30/90/365 يوم).

**الرجوع (rollback):** يكفي ترجّع الكود القديم. الجداول والأعمدة الجديدة مش بتأثر على الكود القديم، ومفيش داعي تمسحها.

---

## 4. الافتراضات والقرارات

1. **«فتح CP»** معناه صلاحية إرسال دعوة CP (وبالتالي عمل ارتباط)، والرسوم بتتدفع **مرة واحدة** لكل حساب.
2. **الـ spec الأصلي كان مقطوع عند «بعد موافقة…».** التفسير المتفق عليه: بعد التأكيد تتخصم الكوينز ذريًا، ويتفتح CP، وتتسجّل العملية. ولو الرصيد مش كافي تظهر رسالة بالناقص ومفيش خصم.
3. **التطبيقات القديمة شغّالة زي ما هي:** طول ما السياسة «مجاني»، السيرفر بيسجّل الفتح تلقائيًا ساعة الدعوة.
4. **المنح من اللوحة ما بيخصمش كوينز.** «فتح CP برسوم» معناه إن المستخدم نفسه هو اللي بيدفع السعر ده لما يأكد من التطبيق. أما «فتح CP له مجانًا الآن» فبيفتحه فورًا.
5. **قفل CP لمستخدم** بيمنع الدعوات الجديدة بس. الارتباطات الحالية بتفضل، ومفيش استرجاع لأي رسوم.
6. **قيمة CP** بتزيد **من هدايا CP المفعّلة بس** (هدية القبول وأي هدية CP بعدها بين الشريكين)، بمقدار «رفع مستوى CP» بتاع كل هدية × العدد (فاضي = سعر الهدية). الهدايا العادية ما بتزوّدهاش. **المستوى** = `1 + floor(القيمة / قيمة رفع المستوى)` بحد أقصى `cp_level_max`، ولو قيمة الرفع 0 بيتحسب بالأيام. تثبيت المستوى من الأدمن بيغلب الاتنين.
7. **المستخدم الظاهر** اختيار **صاحب الصفحة**، والزائر بيشوف نفس اختياره. **أي ارتباط جديد ما بياخدش مكانه أبدًا.** أول ارتباط بيتثبّت تلقائيًا لو المستخدم ما اختارش حد. ولو الارتباط الظاهر اتفك، بيظهر أحدث شريك فاضل.
8. **سحب الخلفية** بيمسح صف (المستخدم، الخلفية) للمستخدم ده بس، ولو كانت مركّبة على صفحته بتتشال. **حذف الخلفية** بيشيلها من كل اللي بيملكوها (نفس سلوك حذف منتج من المتجر)، وعدد الملّاك بيتسجّل في الـ audit.
9. **قيمة CP مش ظاهرة في التطبيق.** التطبيق بيقراها بس ما بيعرضهاش، عشان ما نغيّرش تصميم الكارت من غير موافقة العميل.

---

## 5. إيه اللي اتشغّل وإيه اللي اتسكّب

| البند | الحالة |
|---|---|
| `npx tsc --noEmit` (الـ backend) | ✅ نجح |
| `npm test` (الـ backend) | ✅ 70/70 |
| `prisma validate` | ✅ الـ schema سليم |
| تشغيل الـ migration على PostgreSQL | ⏭️ **اتسكّب**: مفيش PostgreSQL على الجهاز |
| لوحة التحكم (التبويبين) | ✅ اتجرّبت في المتصفح على **API وهمي** بيحفظ حالته: كل النداءات والـ bodies صح، والتأكيد والإلغاء، والتحقق من المدخلات، والـ HTML escaping. **ما اتجرّبتش على سيرفر حقيقي** |
| تطبيق Flutter | ⏭️ **ما اتعملوش compile ولا تشغيل**: مفيش Flutter SDK. الكود اتراجع سطر بسطر بس |
| `flutter test` (`cp_unlock_models_test.dart`) | ⏭️ اتكتب ولسه ما اشتغلش |
| تحميل `cp.routes.ts` في الاختبارات | ⏭️ `bcrypt` (native) مش متبني على الجهاز، فاختبار المسارات بيقرا الملف كنص. `setFeaturedPartner` (بتاعة التطبيق) من غير اختبار مباشر، والقاعدة نفسها متغطية في نسخة الأدمن |

---

## 6. خطوات النشر

> **قبل أي حاجة:** خُد نسخة احتياطية من الداتابيز: `pg_dump`.

### على بيئة الاختبار (staging) الأول

1. **الكود:** `git fetch && git checkout feat/cp-permissions-admin`
2. **الـ backend:** السكربت `backend/deploy.sh` بيعمل الخطوات دي بالترتيب:
   ```bash
   cd backend
   npm install
   npx prisma generate
   npm run build
   npx prisma migrate deploy      # مش db push
   pm2 restart samafox-api
   ```
3. **اتأكد إن الـ migration اتطبّق:**
   ```sql
   SELECT COUNT(*) FROM cp_unlocks WHERE source = 'legacy';   -- ≥ عدد اللي عندهم زوج CP (وبيشمل اللي بعتوا طلب اتقبل)
   SELECT key, value FROM app_settings WHERE key LIKE 'cp_%';
   ```
4. **التطبيق** (على جهاز فيه Flutter SDK):
   ```bash
   cd app
   flutter pub get
   flutter analyze
   flutter test
   flutter build apk --release
   ```
   ونزّل الـ APK على أجهزة التجربة. اتأكد إن النسخة دي متوجّهة لسيرفر الـ staging مش الإنتاج.

### خطة التجربة على الـ staging

**من غير ما تغيّر أي إعداد (لازم ما يتغيّرش أي حاجة):**
- [ ] مستخدم عنده CP قبل كده: الكارت بنفس المستوى والأيام، والمستخدم الظاهر زي ما هو.
- [ ] نسخة التطبيق **القديمة**: إرسال هدية CP شغّال عادي من غير أي رسالة جديدة.
- [ ] مستخدم جديد يبعت CP: مفيش أي dialog، والدعوة بتروح.

**سياسة برسوم** (من اللوحة: CP → سياسة الفتح → برسوم 500):
- [ ] مستخدم جديد رصيده 1000 يبعت CP: dialog فيه «الرسوم 500، رصيدك 1000». «إلغاء» ما بيخصمش حاجة. «موافق» بيخصم 500 مرة واحدة، والدعوة بتروح، والرصيد اللي بيظهر 500.
- [ ] مستخدم رصيده 100: الـ dialog بيقول «ينقصك 400 كوينز» ومفيش زرار دفع، والرصيد ما يتغيّرش.
- [ ] نسخة التطبيق القديمة مع سياسة برسوم: الدعوة بترجع برسالة «فتح الـ CP يتطلب 500 كوينز» ومفيش خصم.
- [ ] مستخدم عنده CP قبل كده (legacy): ما بيطلبش منه رسوم.

**صلاحيات فردية:**
- [ ] «فتح CP مجانًا» لمستخدم والسياسة برسوم: بيبعت من غير رسوم.
- [ ] «فتح CP برسوم 120» لمستخدم: الـ dialog بيعرض 120.
- [ ] سحب الصلاحية: بيرجع لسياسة النظام، وسجل العمليات فيه المنح والسحب بالقيم.

**المستوى والمستخدم الظاهر:**
- [ ] «قيمة رفع مستوى CP» = 1000: زوج قيمته 2500 يبقى LV.3 في التطبيق.
- [ ] تثبيت مستوى زوج من اللوحة: التطبيق يعرضه.
- [ ] تغيير المستخدم الظاهر من التطبيق (النجمة) أو من اللوحة: زائر الصفحة يشوف نفس الاختيار.

**الخلفيات:**
- [ ] رفع خلفية مدفوعة، ثم منحها لمستخدم: تظهر عنده من غير خصم.
- [ ] سحبها من مستخدم واحد: تتشال من صفحته بس، ومستخدم تاني بيملكها ما يتأثرش.
- [ ] محاولة وضع خلفية مدفوعة مش مملوكة من التطبيق بتترفض بـ `BACKGROUND_NOT_OWNED`.

**سجل العمليات:**
- [ ] كل خطوة فوق ليها صف فيه المسؤول والعملية والمستخدم وقبل/بعد والـ IP.

### للإنتاج (production)
بعد ما كل اللي فوق يعدّي على الـ staging: نفس الخطوات، والإعدادات تفضل «مجاني» لحد ما العميل يقرر. **ارفع نسخة التطبيق الجديدة على المتجر قبل ما تحوّل السياسة لـ«برسوم»**، عشان النسخ القديمة بتعرض رسالة خطأ بدل dialog الدفع.

---

## 7. متابعات معروفة

- تشغيل `flutter analyze` / `flutter test` وتجربة التطبيق فعليًا. **دي أهم خطوة قبل النشر.**
- تشغيل الـ migration على نسخة من داتابيز الإنتاج على الـ staging.
- اختبار مباشر لـ `setFeaturedPartner` ومسارات `/cp` بعد بناء `bcrypt` (`npm rebuild bcrypt`).
- عرض «قيمة CP» في كارت العلاقة لو العميل عايز.
- جدول الخلفيات في اللوحة بيتسكرول بالعرض على الشاشات الضيقة، زي باقي جداول اللوحة.
- **لو السيرفر وقع في نص قبول دعوة:** الدعوة ممكن تفضل في حالة `accepting`. الحالة دي نادرة، ومحتاجة مراجعة يدوية لو حصلت.
- **`POST /store/send` (إهداء منتج من المتجر لمستخدم):** بيدّي المستلم نسخة **أبدية** حتى لو المنتج بمدة. ده سلوك قديم، برّه نطاق الميزات دي، ومحتاج قرار.

---

## 8. تحديث 2026-09-24 — الميزات التلاتة: «خلفيات خاصتي»، «تحسين عرض CP»، «هدايا CP»

### 8.1 خلفيات خاصتي

| # | المطلوب | الحالة وإزاي اتعمل |
|---|---|---|
| 1 | قائمة بخلفيات المستخدم بس | ✅ شاشة جديدة **«خلفيات خاصتي»** (`app/lib/screens/my_backgrounds_screen.dart`)، بتتفتح من «تعديل الملف ← خلفية الصفحة الشخصية». وتبويب المخزن في البروفايل اسمه بقى «خلفيات خاصتي» ورسالته بالعربي. الاتنين بيجيبوا البيانات من `GET /store/inventory`، اللي بيرجّع صفوف المستخدم نفسه بس، ومن غير المنتهية |
| 2 | الشراء بيضيفها تلقائيًا | ✅ `POST /store/buy` بيعمل صف `user_items` للمشتري (أو بيمد المدة لو بمدة) |
| 3 | المنح بيضيفها للمستخدم ده بس | ✅ `grantBackground`، ومتغطي باختبار |
| 4 | ما تظهرش لحد تاني | ✅ الاستعلام بـ `userId` صاحب الطلب. ومتغطي باختبار: السحب من مستخدم ما بيأثرش على غيره |
| 5 | الظهور في المتجر مش ملكية | ✅ الملكية = صف `user_items` بس. والمجانية كمان لازم تتاخد الأول. و`PUT /users/me` بيرفض خلفية متجر مش مملوكة |
| 6 | الملكية في الداتابيز ومربوطة بالمستخدم | ✅ جدول `user_items` (`userId` + `itemId`) |
| 7 | جهاز تاني بيجيب الملكية من السيرفر | ✅ القائمة بتتجاب من السيرفر كل مرة بتتفتح، ومفيش أي نسخة على الجهاز |
| 8 | (البند مقطوع: «عند حذف التطبي…») | ✅ فهمته كده: بعد حذف التطبيق وتنزيله تاني الخلفيات بتفضل موجودة، لأنها على السيرفر زي البند 7 |
| + | إصلاح اتلقى أثناء المراجعة | `POST /store/activate` كان بيسمح بتركيب منتج مدته خلصت قبل ما التنظيف الدوري يمسحه. دلوقتي بيرفضه |

### 8.2 تحسين عرض CP

| # | المطلوب | الحالة |
|---|---|---|
| 1–3 | الاختيار بيتحفظ في الداتابيز ومربوط بصاحب الحساب، وأي زائر بيشوفه | ✅ `users.cpFeaturedPartnerId`، و`GET /cp/partners/:userId` بيرجّع `featured` |
| 4 | ما يتبدّلش بآخر حد اتبعتله CP | ✅ **اتصلّح.** كان فيه ثغرة: لو المستخدم ما اختارش على السيرفر، كان بيظهر أحدث ارتباط. الحل: <br>• **الـ migration** بيثبّت لكل مستخدم الشريك اللي بيظهر للزوار النهارده.<br>• **ارتباط جديد** بيتثبّت بس لو المستخدم مالوش اختيار (`pinFeaturedIfUnset`).<br>• **فك الارتباط الظاهر** بيحوّل لأحدث شريك فاضل.<br>• **الاختيار القديم اللي كان على الموبايل** بيترفع للسيرفر مرة واحدة ويتمسح من الجهاز. |
| 5 | تغيير الاختيار بيوصل لكل الزوار | ✅ `PATCH /cp/featured` من التطبيق، أو من اللوحة |
| 6 | عرض متسق بين التطبيق والسيرفر | ✅ التطبيق بيعرض اختيار السيرفر بس |
| 7 | (البند مقطوع: «منع الاعتماد على آخر عم…») | ✅ مفيش أي مكان بيعتمد على «آخر عملية» في الاختيار |
| مثال | A اختار B، وبعدين عمل CP مع C | ✅ B يفضل ظاهر، ومتغطي باختبار بالمثال ده بالظبط |

### 8.3 هدايا CP

| # | المطلوب | الحالة |
|---|---|---|
| 1 | قسم مستقل في اللوحة | ✅ «هدايا CP» في تبويب CP |
| 2 | تفعيل وتعطيل | ✅ والهدية المعطّلة ما بتعملش ارتباط ولا بترفع مستوى |
| 3 | قيمة كل هدية | ✅ السعر |
| 4 | مقدار رفع مستوى CP لكل هدية | ✅ **جديد:** `gifts.cpLevelPoints` وعمود «رفع مستوى CP» في اللوحة (فاضي = نفس السعر) |
| 5 | إرسال هدية CP بيحدّث المستوى | ✅ **اتصلّح.** قبل كده، هدية CP لشريك موجود كانت بترجع `ALREADY_PAIRED`، ومفيش طريقة ترفع المستوى من هدايا CP. دلوقتي بتتبعت على طول وبتزوّد القيمة. **وبقت هدايا CP بس اللي بترفع المستوى** (قبل كده كانت كل الهدايا بسعرها) |
| 6 | سجل الإرسال والقيمة الناتجة | ✅ جدول `cp_value_events` (الهدية، العدد، +CP، القيمة بعدها)، وبيظهر في «بيانات CP لمستخدم» في اللوحة |
| 7 | منع الاحتساب مرتين | ✅ **ثلاث طبقات:**<br>• `giftTransactionId` فريد: نفس الهدية ما تتحسبش مرتين.<br>• `requestKey` لكل ضغطة: إعادة إرسال نفس الطلب ما بتتخصمش تاني.<br>• حجز ذري للدعوة قبل الخصم: ضغطتين على «قبول» أو «رفض» ما بيخصموش مرتين. ده كان **سباق حقيقي** في الكود القديم واتصلّح |
| 8 | التحقق من السيرفر | ✅ السعر ومقدار الرفع ونوع الهدية وحالتها كلهم بيتقروا من الداتابيز. التطبيق ما بيبعتش أي رقم منهم، ومتغطي باختبار |
| 9 | النتيجة تظهر للمستخدمين | ✅ **المرسل:** «+X CP» أو «ارتفع مستوى الـ CP إلى LV.n».<br>**المستلم:** إشعار.<br>**الزوار:** المستوى الجديد في كارت العلاقة |
| 10 | التعديل من اللوحة من غير تحديث التطبيق | ✅ كل القيم بتتقري من السيرفر ساعة الإرسال |

**ملاحظة للنسخ القديمة من التطبيق:** هدية CP لشريك موجود من نسخة قديمة **بتنجح دلوقتي** وبترفع المستوى، لكن النسخة القديمة هتعرض «في انتظار الرد» والرصيد مش هيتحدّث لحد ما التطبيق يعمل refresh.

### 8.4 خطوات تجربة إضافية على الـ staging

- [ ] **خلفيات خاصتي:** اشترِ خلفية واتأكد إنها ظهرت في «خلفيات خاصتي»، وإنها ما ظهرتش عند مستخدم تاني. سجّل دخول من جهاز تاني والقائمة تبان نفسها.
- [ ] **المستخدم الظاهر:** A عنده B ظاهر. A يعمل CP مع C، والزائر لصفحة A يفضل شايف B.
- [ ] **هدايا CP:**
  - حط «رفع مستوى CP» = 300 لهدية، وابعتها مرتين لشريك: القيمة +600.
  - السجل في اللوحة بيعرض الصفين.
  - المستوى بيتغيّر حسب «قيمة رفع مستوى CP».
- [ ] **التعطيل:** عطّل هدية CP من اللوحة، والتطبيق ما يقدرش يبعتها.
