# تسليم الـ77 بند — SamaFox

**الفرع:** `audit-remaining-items` (7 كوميتات فوق `greedy-cat-jackpot`)
**التاريخ:** 10/09/2026 · **الحجم:** 116 ملف، +12,179 / −769 سطر

---

## 1. الوضع النهائي

| | قبل | بعد |
|---|---|---|
| ✅ منفّذ بالكامل | 39 | **69** |
| 🟡 جزئي | 18 | 1 |
| ❌ لم يُنفّذ | 17 | 0 |
| 🔧 الكود جاهز — محتاج تنفيذ منك | — | **7** |

الـ7 دول مش ناقصين كود. الكود اتكتب وموجود في الفرع؛ اللي ناقص خطوة على السيرفر
أو في Play Console مش ممكن تتعمل من هنا. تفاصيلهم في القسم 3.

---

## 2. البنود اللي اتقفلت

### الصوت والصلاحيات

| البند | كان | اتعمل إيه |
|---|---|---|
| **A2** الصوت (×12) | ❌ | `build-release.sh` بيمرّر `TURN_URLS` **ويرفض** يبني release من غيرها. السبب الجذري كان إن الإعداد افتراضيه فاضي ومفيش سكربت واحد بيمرّره |
| **A3** السماعة (×3) | ❌ | `services/audio_route.dart` — مصدر واحد للاختيار. WebRTC بقى يقراه بدل ما يفرض السماعة، وزر في قائمة الغرفة بيكتبه، وكل مشغّلات صوت الألعاب مسجّلة فيه |
| **A9** الصعود التلقائي | ❌ | اتشالت كتلة الـauto-seat كاملة من `socket.service.ts` |
| **A10** منع إطفاء الشاشة | ❌ | `wakelock_plus` على مستوى التطبيق، بيتفك لما يروح للخلفية |
| **A11** صوت التسجيل | ❌ | مسجّل شاشة داخل التطبيق. **اقرأ القسم 4** — فيه قيد من أندرويد نفسه |
| **A12** مكان الكأس | 🟡 | «إجراءات الغرفة» اتنقلت جوه «إعدادات الغرفة» |
| **A14** إجمالي دعم الروم (×3) | ❌ | نافذة زمنية متحركة (مش عدّاد بيتصفّر بمهمة مجدولة) + اختصار k/m + المدة من اللوحة |
| **A15** نظام المكافآت | ❌ | مكافأة كأس الروم (تلقائية، لوكالة المضيفين) + مكافأة الداعمين بزر «مكافأة لك» + قسم في اللوحة |
| **A16** غرفة 100000 | ❌ | مثبّتة أول القائمة وبكارت كبير |
| **A17** الحظر (×4) | 🟡 | 3 بوابات كانت بتفحص `isAdmin` لوحده وبتقفل على السوبر أدمن — اتوسّعت. والحظر بقى يطرد فوراً بدل ما يفضل قاعد |
| **A19** تعيين المشرف | ❌ | **خطأ منطقي مؤكّد:** `getRoomRole` بترجّع `'super'` و`addRoomAdmin` بتقبل `'owner'`/`'admin'` بس. بقى بالرتبة عبر `rankOf()` |
| **A8** أيقونة الموسيقى | 🟡 | الشريط بقى لصاحب التشغيل نفسه (`hostId` كان موجود ومهمل) |

### الوكالات والاقتصاد

| البند | كان | اتعمل إيه |
|---|---|---|
| **B6** تارجيت وكيل الشحن | 🟡 | الشحن بقى ينزّل تارجيت بقيمته كاملة وبدون نسبة، جوه نفس المعاملة |
| **B8** شاشة وكالتي | ❌ | صاحب الوكالة بيشوف وكالته بس؛ العادي بيشوف الكل |
| **B13** تعديل التارجيت (×2) | 🟡 | endpoint جديد بالمستخدم (Display ID أو الأيدي الداخلي) بدل ما يكون متاح من جوه قائمة أعضاء الوكالة بس |
| **B14** تتبع الشحنات (×2) | 🟡 | بحث بسجل شحنات المستخدم (شحن كام ومن مين) |

### المظهر والليفل

| البند | كان | اتعمل إيه |
|---|---|---|
| **C6** الفقاعات (×3) | 🟡 | فقاعة في الرسائل الخاصة. helper واحد في السيرفر + widget واحد في التطبيق للاتنين، والانتهاء فلتر في الاستعلام فبتختفي من الاتنين في نفس اللحظة |
| **C11** الشارات (×5) | 🟡 | `?onlyBadges=1` — السطر اللي كان بيخلط الشارات بباقي المنتجات |
| **C16** المتابعة والزوار | ❌ | شاشة بأربع تبويبات + تسجيل زيارات من الرئيسية فقط + بوابة ليفل من اللوحة |
| **C17** مكان CP (×3) | 🟡 | اتنقل من صفحة الغرف للصفحة الشخصية فوق الهدايا الممنوحة بالظبط |
| **C18** صور الخاص | 🟡 | منتقي صور + رفع + إرسال، وبوابة VIP بتتفحص في السيرفر |
| **E2** مصادر شحن الـVIP | 🟡 | مسارين في اللوحة كانوا بيحرّكوا `coinsBalance` من غير `totalRecharge` — والـVIP بيتحسب من التاني |
| **E3** الترقية اليدوية | ❌ | رفع الـVIP يدوي بقى يرفع `totalRecharge` لأرضية الدرجة، والليفل بيرفع `xp` — فالدرجة الجاية بتطلب الفرق مش التراكمي |

### لوحة التحكم والنشر

| البند | كان | اتعمل إيه |
|---|---|---|
| **D6** شريط الهدية | 🟡 | اتنقل لليمين (بـ`Alignment.centerRight` الفيزيائي — الغرفة RTL و`start` فيها هو اليمين) |
| **F3** رسائل الإدارة | ❌ | إرسال للجميع أو لمستخدم بالأيدي، من قسم جديد في اللوحة |
| **F4** حظر الجهاز (×2) | ❌ | موديل + إنفاذ قبل **كل** طرق الدخول **والتسجيل** — حظر بيتهرب منه بحساب جديد مش حظر |
| **G1/G6** الأداء والحجم (×5) | ❌ | R8 + shrinkResources + `proguard-rules.pro` + البناء بقى `appbundle` |
| **G2** صفحة التحميل (×9) | ❌ | `download.html` + endpoint بيقرا الإصدار من ملف الـAPK نفسه |
| **G3** الألعاب (×8) | 🟡 | الـ4 المخفيين رجعوا (11 لعبة كلهم شغالين) + قسم ألعاب في اللوحة (تشغيل/إيقاف + حدود رهان) + صوت الألعاب بقى يتبع السماعة |
| **G5** ASO | ❌ | اسم التطبيق بقى «سما فوكس» + `docs/aso-plan.md` |

---

## 3. السبعة اللي محتاجة إيدك

الكود جاهز في الفرع. دي الخطوات اللي مش ممكن تتعمل من جهاز تطوير.

### 3.1 🔴 نصب TURN — أهم خطوة في التسليم كله
من غيرها بند A2 لسه مكسور مهما كان الكود مظبوط.

```bash
sudo apt install coturn
```
`/etc/turnserver.conf`:
```
listening-port=3478
tls-listening-port=5349
fingerprint
lt-cred-mech
user=samafox:اختر_باسورد_قوي
realm=samafox
```
افتح في الـSecurity Group: **UDP+TCP 3478**، **TCP 5349**، **UDP 49152-65535**.
بعدها:
```bash
cd app && cp turn.env.example turn.env   # واملا البيانات
```

### 3.2 🔴 نشر الباك إند
```bash
cd backend && ./deploy.sh
```
**خد نسخة احتياطية لقاعدة البيانات الأول.** فيه 3 ميجريشنات لازم تشتغل مرة واحدة
بالظبط، وميجريشن جديد (`20260910000000`) بيضيف جداول المكافآت وحظر الجهاز والزوار.

### 3.3 🔴 بناء التطبيق
```bash
cd app && ./build-release.sh          # bundle للمتجر
cd app && ./build-release.sh apk      # نسخة التحميل المباشر
```
⚠️ **الـKotlin بتاع A11 لم يُصرَّف.** الجهاز ده مفيهوش Gradle cache خالص و7 جيجا
فاضيين على C:. لو البناء وقع، هيقع في `ScreenRecordService.kt` أو `MainActivity.kt`
وبس — باقي الفرع مالوش علاقة.

### 3.4 🟠 رفع الـAPK لصفحة التحميل
```bash
cp app/build/app/outputs/flutter-apk/app-release.apk \
   backend/public/downloads/samafox-1.0.18.apk
```
الصفحة بتقرا الإصدار من اسم الملف — مفيش رقم مكتوب في أي مكان تاني.

### 3.5 🟠 حساب اللوحة
العميل شايف «أنت أدمن عادي» لأن حسابه `isSuperAdmin=false`:
```bash
cd backend && npx ts-node prisma/grant-admin.ts
```
ولو حساب السوبر أدمن `isAdmin=false`، خلّيه `true` كمان — الكود بقى يقبل الاتنين
بس البيانات لسه محتاجة تتظبط.

### 3.6 🟠 فك قفل بيع/تبديل التارجيت
`GLOBAL_DEFAULT_BLOCKED = true` — أي قاعدة بيانات مفيهاش صف الإعداد بتكون مقفولة
كلياً، وده سبب اختفاء الأزرار (B11). من اللوحة: **الإعدادات ← بيع التارجيت ← فك المنع**.

### 3.7 🟠 Play Console
- إقرار `FOREGROUND_SERVICE_MICROPHONE` **و** `FOREGROUND_SERVICE_MEDIA_PROJECTION`.
  الحساب ده اترفض له تطبيق قبل كده بسبب النقطة دي بالظبط.
- اللغة الأساسية **عربي**، والعنوان والوصف من `docs/aso-plan.md`.
- ارفع الـ`.aab` مش الـ`.apk`.

---

## 4. حاجة لازم تتقال بصراحة عن A11

العميل بيسجّل بمسجّل الموبايل وبيطلع من غير صوت. **ده مش بق في التطبيق، وأي كود
في الدنيا مش هيصلحه.**

أندرويد بيمنع تسجيل أي stream نوعه `USAGE_VOICE_COMMUNICATION`، وWebRTC بيستخدم
النوع ده بالظبط لصوت الغرفة. نفس القاعدة اللي بتمنع أي تطبيق يسجّل مكالمة تليفون.

فالحل الوحيد الممكن هو تسجيل **جوه التطبيق** من الميكروفون، وده اللي اتعمل. اللي
بيتسجّل فعلاً:
- **صوت المستخدم نفسه** — دايماً وبوضوح.
- **باقي الغرفة** — لما السماعة الخارجية مفعّلة، لأن أصواتهم بتطلع من السماعة
  وترجع تاني من المايك. على سماعة الأذن هيكونوا خافتين أو مش موجودين.

التطبيق بيقول للمستخدم الكلام ده **قبل** ما يبدأ التسجيل، مش بعده.

---

## 5. التحقق اللي اتعمل

| | النتيجة |
|---|---|
| `tsc --noEmit` (الباك) | **0 أخطاء** |
| `flutter analyze` | **0 أخطاء**، 142 تحذير، 1872 info |
| التحذيرات الـ142 | **كلها موجودة قبل الشغل ده** — العدد واحد بالظبط قبل وبعد |
| `node --check` (اللوحة) | نجح على الـbundle-ين |
| `prisma validate` | صحيح |
| XML (المانيفست + strings) | صحيح |
| `bash -n` | نجح على `build-release.sh` و`deploy.sh` |
| Kotlin | ❌ **لم يُصرَّف** — القسم 3.3 |
| على جهاز حقيقي | ❌ لم يُشغَّل |

**تصحيح:** في أول كوميت قلت «0 warnings». ده كان غلط — grep مش مطابق لصيغة مخرجات
الـanalyzer. الرقم الصح 142 تحذير، كلها سابقة لشغلي، واتأكدت من ده بمقارنة العدد
قبل وبعد.

---

## 6. ملاحظتان على الفرع

1. **ملفات مش بتاعتي دخلت في الكوميتات.** `git add -A app/lib` شال معاه ملفات
   `asterion_*` و`image_opaque_bounds.dart` و`app_network_image.dart` اللي كانت
   untracked من شغل سابق في الشجرة. سبتها لأن الفرع محتاجها عشان يبني — من غيرها
   ملفات متتبّعة بتستورد ملفات مش موجودة في git.

2. **`main` لسه متجمّد من 04/07.** الفرع ده فوق `greedy-cat-jackpot`. الشغل كله —
   بتاعي وبتاع دفعة الـ39 — لسه مش في `main`. لو النشر بيتم من `main`، لازم
   الدمج يحصل الأول وإلا مش هيوصل حاجة للعميل.
