# تدقيق تقني كامل — SamaFox
### طلبات العميل من 01/08/2026 إلى 10/09/2026

**تاريخ التدقيق:** 10/09/2026 · **الفرع المفحوص:** `greedy-cat-jackpot` (آخر كوميت `7373082` — 05/09/2026)
**النطاق:** Flutter app (226 ملف dart) · Node/TypeScript backend (109 ملف ts) · لوحة تحكم HTML/JS ثابتة
**عدد البنود:** 77 بند مستخرج من الشات

---

## 1. الملخص التنفيذي

**النسبة: 39 بند ✅ منفّذ بالكامل (51%) · 18 بند 🟡 جزئي (23%) · 17 بند ❌ لم يُنفّذ (22%) · 3 بنود ❓ (4%).**

الكود أفضل بكثير مما توحي به شكاوى العميل. دفعة 26/08 (الـ39 تعديل) نُفّذت فعلاً وبجودة عالية: عمولة الوكيل تذهب للتارجيت، فروع وكالة الشحن تشحن من محفظتها، الهدية على النفس تُحتسب كاملة، ضبط الإطارات بقياس البكسل غير الشفاف، الشارات التلقائية، نظام الموسيقى كامل، الهدية الجماعية بخصم مجمّع. **المشكلة ليست في كتابة الكود — بل في وصوله للعميل.**

**أخطر 5 فجوات:**

1. **`main` متجمّد منذ 04/07/2026.** كل شغل أغسطس وسبتمبر (81 كوميت) موجود على فروع غير مدموجة. لو النشر يتم من `main`، فالعميل لم يستلم أي تعديل من دفعة الـ39 أصلاً — وهذا يفسّر تكراره لبنود قيل إنها اتعملت.
2. **A2 — الصوت (×12): سيرفر TURN غير مُهيّأ إطلاقاً.** `TURN_URLS` قيمته الافتراضية سلسلة فارغة ولا يوجد أي سكربت بناء يمرّرها. كل نسخة release تعمل بـ STUN فقط، ومستخدمان على بيانات الموبايل (CGNAT) لا يتصلان أبداً. التعليق في الكود نفسه يسمّي هذا "السبب الأكبر لـ الصوت بيفصل" — ولم يُوصَّل.
3. **A19 — تعيين المشرف: خطأ منطقي مؤكّد.** `getRoomRole` تُرجع `'super'` للسوبر أدمن، بينما `addRoomAdmin` تقبل `'owner'` أو `'admin'` فقط ⇒ 403 دائماً. حتى صاحب الغرفة لو كان سوبر أدمن يُرفض.
4. **A17 — الحظر: كل مسارات `/admin` محجوبة بـ `isAdmin` وحده.** حساب سوبر أدمن بـ `isSuperAdmin=true` و`isAdmin=false` يُرفض في الميدل وير وفي واجهة التطبيق معاً — الزر يظهر ولا يفعل شيئاً.
5. **G1/G6 — حزمة الإصدار 161 ميجابايت.** بلا `isMinifyEnabled` ولا `isShrinkResources` ولا تقسيم ABI ولا ملف AAB. تتجاوز حد Google Play (150MB) وتناقض مباشرةً مطلب "استهلاك بيانات قليل".

**الحالة العامة لكل موديول:**

| الموديول | الحالة | ملاحظة |
|---|---|---|
| الوكالات والتارجيت (B) | 🟢 ممتاز — 9/15 ✅ | أنظف كود في المشروع؛ معاملات ذرّية وحراسات سباق سليمة |
| المظهر والبروفايل (C) | 🟢 جيد — 12/18 ✅ | ناقص: الزوار، مكان CP، فقاعة الخاص |
| الهدايا والإشعارات (D) | 🟢 جيد — 7/9 ✅ | مكتمل تقريباً |
| الليفل والـVIP (E) | 🟡 متوسط | E1 اتصلّح فعلاً؛ E3 مرفوض بقرار تصميمي |
| الصوت والغرف (A) | 🔴 ضعيف — 8/21 ✅ | أخطر قسم، وأكثره لم يُلمس بعد 26/08 |
| لوحة التحكم (F) | 🟡 متوسط | حظر الجهاز ورسائل الإدارة غير موجودين |
| الأداء والنشر والألعاب (G) | 🔴 ضعيف | لا صفحة تحميل، لا ASO، لا لوحة ألعاب، هامش بيت 3% |

**ملاحظة زمنية حاسمة:** بين 26/08 و10/09 (فترة كل شكاوى العميل المتكررة) صدر **20 كوميت، كلها ألعاب**. ولا كوميت واحد على الصوت أو الصلاحيات أو لوحة التحكم. البنود التي كررها العميل بعد 26/08 لم يُعمل عليها.

---

## 2. الجدول الرئيسي

### القسم A — الصوت والغرف

| # | العنوان | الحالة | الدليل | ملاحظة |
|---|---|---|---|---|
| A1 | ثبات المستخدم في الغرفة | 🟡 | `app/lib/services/room_audio_keepalive.dart:36` · `AndroidManifest.xml:95` | الخدمة الأمامية موجودة وموصولة؛ القطع المتبقي سببه A2 وعدم طلب إذن الإشعارات |
| A2 | استقرار الصوت (×12) | ❌ | `app/lib/config/app_config.dart:41` `defaultValue: ''` | WebRTC mesh بلا TURN — أخطر بند في التدقيق كله |
| A3 | السماعة / الإيربيس (×3) | ❌ | `webrtc_audio_service.dart:438-441` | `_applyEchoSafeMode` تفرض السماعة دائماً وتتجاهل اختيار المستخدم |
| A4 | دائرة المتكلم (×3) | ✅ | `app/lib/widgets/room/seat_card.dart:94-95, 207` | VAD من `media-source` stats موصولة للسوكت |
| A5 | حد المايكات نصف الشاشة | ✅ | `app/lib/widgets/room/seats_grid.dart:108` | `size.height * 0.5` مع حلّ تخطيط يقلّص المقاعد |
| A6 | المايك الكامل / جميع الغرفة | ✅ | `gift_picker_sheet.dart:363` · `backend/src/gifts/controller.ts:425` | تحديد جماعي + `grandTotal` مجمّع |
| A7 | نظام الموسيقى | ✅ | `backend/src/routes/music.routes.ts:67` · `music_player_bar.dart:83` | رفع/حذف/قائمة دائمة/شريط قابل للسحب/صلاحية |
| A8 | أيقونة الموسيقى للمشغّل فقط | 🟡 | `music_player_bar.dart:66` · `room_screen.dart:2280` | مقصورة على المشرفين لا على من شغّلها؛ الأيقونة العلوية للجميع |
| A9 | إلغاء الصعود التلقائي | ❌ | `backend/src/services/socket.service.ts:1432` `if (admins.has(uid))` | لم يُلمس — لا يزال يصعد الأدمن والمالك تلقائياً |
| A10 | منع إطفاء الشاشة | ❌ | لا يوجد wakelock في pubspec ولا `WAKE_LOCK` في المانيفست | غير منفّذ إطلاقاً |
| A11 | صوت تسجيل الشاشة | ❌ | لا `MediaProjection` ولا خدمة تسجيل | لا يوجد مسجّل شاشة داخل التطبيق أصلاً |
| A12 | كأس الغرفة + اسم متحرك | 🟡 | `room_screen.dart:4097, 4139` · `home_screen.dart:1188` | Marquee ✅ والكأس في الهيدر ✅، لكن "إجراءات الغرفة" لم تُنقل |
| A13 | الكأس يتفاعل مع الدعم (×4) | ✅ | `backend/src/gifts/controller.ts:284-308` · `supporters_board_screen.dart:68` | تجميع حقيقي، 30 عام / 20 غرفة، تحديث فوري تحت المايك |
| A14 | إجمالي دعم الروم (×3) | ❌ | لا يوجد أي تجميع لإجمالي الغرفة | غير منفّذ؛ `supportersResetAt` بند مختلف (C15) |
| A15 | نظام المكافآت | ❌ | `admin-dashboard.html:364` تخص B14 فقط | لا مكافأة كأس روم ولا زر "مكافأة لك" |
| A16 | غرفة الإدارة / ID 100000 | ❌ | `backend/src/controllers/room.controller.ts:62` `orderBy: createdAt desc` | لا تثبيت ولا حجم كبير؛ `isFeatured` كود ميت |
| A17 | صلاحيات الأدمن/السوبر (×4) | 🟡 | `backend/src/routes/admin.routes.ts:23` · `room_screen.dart:1252` | المنطق كامل لكنه محجوب بـ `isAdmin` في 3 مواضع |
| A18 | صلاحيات مايك إضافية | ✅ | `room_screen.dart:836, 826` · `room-admin.controller.ts:190` | قفل/فتح/كتم/فك كتم في أي غرفة |
| A19 | تعيين مشرف الغرفة | ❌ | `room-admin.controller.ts:78-79` + `:136-138` | خطأ منطقي مؤكّد — التفاصيل في القسم 3 |
| A20 | فتح البروفايل من التيكست (×3) | ✅ | `widgets/room/room_chat_panel.dart:230, 468` | يفتح نفس شيت المقعد بالأيدي والليفل والـVIP |
| A21 | فتح البروفايل من الرسائل | ✅ | `messages_screen.dart:246` · `chat_screen.dart:375` | |

### القسم B — الوكالات والتارجيت

| # | العنوان | الحالة | الدليل | ملاحظة |
|---|---|---|---|---|
| B1 | عمولة الوكيل للتارجيت | ✅ | `backend/src/gifts/giftService.ts:199-206` | `commissionTargetCoins` لا كوينز |
| B2 | الوكيل يرمي على نفسه (×3) | ✅ | `agency.controller.ts:1278-1286` | فلتر `senderId != userId` أُزيل عمداً |
| B3 | شرط صرف عمولة المضيفين | ✅ | `agency.controller.ts:1417-1428` | حجز لكل مصدر حتى يكمل صاحبه تارجيته |
| B4 | كوينز الوكالة في المحفظة (×2) | ✅ | `agency.controller.ts:196, 215` | `funderId = senderId`، رصيد الوكالة صار إحصاء فقط |
| B5 | فروع وكالة الشحن (×4) | ✅ | `agency.controller.ts:1153, 196-215` | إضافة/حذف/عرض + الشحن من محفظة الفرع |
| B6 | تارجيت وكيل الشحن | 🟡 | `agency.controller.ts:1441-1452` | الهدايا بقيمتها كاملة ✅؛ الشحن لا يمنحه تارجيت |
| B7 | تارجيته مع الاستضافة في اللوحة | ❓ | `adminDashboard.controller.ts:1611` | يحتاج تأكيد على بيانات حقيقية |
| B8 | شاشة "وكالتي" | ❌ | `agency.controller.ts:65, 90` · `charging_agent_screen.dart:1588` | تُرجع كل الوكالات بلا فلترة للمالك |
| B9 | بيع وتبديل التارجيت (×3) | ✅ | `agency.controller.ts:1700` `amount/2` · `targetMembershipWhere` | المضيف والوكيل، بلا حد أقصى |
| B10 | منع البيع لمستخدم بالأيدي (×2) | ✅ | `adminDashboard.controller.ts:728-737` | حلّ فرق displayId/id |
| B11 | اختفاء إجراءات التارجيت | 🟡 | `backend/src/utils/targetLock.ts:20` `GLOBAL_DEFAULT_BLOCKED = true` | المعاملة ذرّية ✅؛ الاختفاء سببه القفل العام الافتراضي |
| B12 | تارجيت غير المسجّل (×2) | ✅ | `agency.controller.ts:1448-1455` | `targetMembershipWhere` يقصره على الأعضاء المعتمدين |
| B13 | تعديل التارجيت من اللوحة (×2) | 🟡 | `adminDashboard.controller.ts:2050, 2076` | يعمل على صف عضوية؛ من ليس عضو وكالة (السوبر أدمن) لا صف له |
| B14 | تتبع الشحنات (×2) | 🟡 | `adminDashboard.controller.ts:972` · `agencyReward.service.ts:69` | عدّاد الشحن الذاتي ✅ والمكافأة التلقائية ✅؛ بحث سجل شحنات المستخدم ناقص |
| B15 | إزالة مضيف بدون رسوم | ✅ | `agency.controller.ts:753` | لا خصم في `removeMember` |

### القسم C — الملف الشخصي والمظهر

| # | العنوان | الحالة | الدليل | ملاحظة |
|---|---|---|---|---|
| C1 | الجنس والعمر (×2) | ✅ | `profile_screen.dart:1159-1164` · `room_screen.dart:5230` | |
| C2 | ترتيب بيانات البروفايل (×3) | ✅ | `room_screen.dart:5300-5316` | الأيدي يسار، الشارات في الصف الفارغ |
| C3 | شكل الليفل والـVIP (×4) | ✅ | `widgets/vip_badge.dart:43-55` | صورة من اللوحة بمقاس يحكمه الكود + fallback مزخرف |
| C4 | شريط الدخولية (×4) | ✅ | `widgets/room/entrance_banner_layer.dart:109` · `utils/image_intrinsic_size.dart` | 9-slice بالمقاس الحقيقي للصورة |
| C5 | مركبة الدخول (×2) | ✅ | `socket.service.ts:1420` · `widgets/user_trail.dart:180` | تُبث للغرفة كلها و`Positioned.fill` |
| C6 | فقاعات الدردشة (×3) | 🟡 | `room_chat_panel.dart:125-140` ✅ · `widgets/chat/message_bubble.dart:62` ❌ | الغرفة ✅؛ الرسائل الخاصة بلا فقاعة |
| C7 | ضبط الإطارات على المايك (×5) | ✅ | `utils/image_opaque_bounds.dart` · `FramedAvatar.dart:131` | قياس البكسل غير الشفاف + `ProductLayout` من اللوحة |
| C8 | خلفية بروفايل المستخدم (×3) | ✅ | `profile_screen.dart:1083` · `user.controller.ts:26` | `PUBLIC_USER_FIELDS` تحملها للزائر |
| C9 | إطار تزيين الصفحة (×2) | ✅ | `profile_screen.dart:1309` · `widgets/profile_decor_frame.dart` | |
| C10 | الصورة المتحركة GIF (×3) | 🟡 | `backend/src/services/animatedAvatar.service.ts:33` | بوابة VIP للأفاتار ✅؛ صورة الغرفة تحتاج تأكيد |
| C11 | نظام الشارات (×5) | 🟡 | `backend/src/controllers/user.controller.ts:548` | **الشكوى قائمة:** `[...badges, ...byType.values()]` تخلط الشارات بمنتجات أخرى عمداً |
| C12 | إظهار الـVIP مع الشارات | ✅ | `room_screen.dart:5302-5304` | صف مستقل غير مشروط بالشارات |
| C13 | تنسيق المتجر | ✅ | `store_screen.dart:217-218` | `mainAxisSpacing: 16` |
| C14 | مدة المنتج عند الشراء | ✅ | `app/lib/models/product.dart:26` | `'أبدي'` أو `'N يوم'` |
| C15 | قائمة "أعلى المستويات" (×2) | ✅ | `supporters_board_screen.dart:129` · `adminDashboard.controller.ts:2195` | التسمية + تصفير غير دائم |
| C16 | المتابعة والزوار | ❌ | `backend/src/follow/follow.routes.ts` موجود · لا موديل زيارات | API المتابعة موجود، التبويبات الأربعة والزوار غير موجودين |
| C17 | نظام CP (×3) | 🟡 | `backend/src/services/cp.service.ts:32, 135, 179` ✅ · `home_screen.dart:16` ❌ | المنطق كامل (30% رفض، تارجيت عند القبول)؛ المكان لا يزال الصفحة الرئيسية |
| C18 | صور وصوتيات في الخاص | 🟡 | `messages.controller.ts:182-183` ✅ · `chat_screen.dart:95` | الصوتيات ✅؛ لا منتقي صور ولا بوابة VIP |

### القسم D — الهدايا والإشعارات

| # | العنوان | الحالة | الدليل | ملاحظة |
|---|---|---|---|---|
| D1 | هدايا الفيديو + صوتها (×5) | ✅ | `gifts/widgets/video_gift_player.dart:60, 72` | `setVolume(1.0)` + `mixWithOthers: true` |
| D2 | تعريب القوائم + CP و VIP | 🟡 | `schema.prisma:1236` `GiftCategory` | البنية موجودة؛ وجود قائمتي CP وVIP فعلياً يحتاج فحص قاعدة البيانات |
| D3 | إدارة قوائم الهدايا (×4) | ✅ | `backend/public/gifts-admin/app.js:19-22` | إنشاء/حذف/نقل إلى قائمة |
| D4 | حذف المنتج من الطرفين (×3) | ✅ | `adminProduct.controller.ts:373-400` | حذف فعلي + تنظيف المراجع المرتبطة |
| D5 | كل المنتجات صورة أو فيديو (×2) | ✅ | `adminProduct.controller.ts:85-86` | القيود لكل نوع أُزيلت |
| D6 | شريط الهدية | 🟡 | `gifts/widgets/gift_announcement_bar.dart:105, 124` | فوق كل شيء ✅ ومزخرف ✅؛ لكنه في المنتصف لا اليمين |
| D7 | شريط الدخولية يغطي | ✅ | `room_screen.dart:4621-4622` | `Positioned.fill` أعلى المكدّس |
| D8 | مدة ومكان الإشعارات | ✅ | `services/global_notification_service.dart:44` | `1500ms` |
| D9 | شريط معرّف الهدية | ✅ | `backend/src/gifts/controller.ts:503` | وسط الشاشة + "إلى الجميع" |

### القسم E — الليفل والـVIP

| # | العنوان | الحالة | الدليل | ملاحظة |
|---|---|---|---|---|
| E1 | الليفل لا يتفاعل (×5) | ✅ | `giftService.ts:249-253` · `xp.service.ts:190-191` | `awardUserXP` لها مُنادٍ واحد فقط: الهدايا، على المُرسِل، بلا هدايا ذاتية |
| E2 | مصادر شحن الـVIP | 🟡 | `adminDashboard.controller.ts:1276` بلا `evaluateVip` | 3 مسارات ✅؛ **إضافة كوينز من اللوحة لا تُغذّي الـVIP** |
| E3 | الترقية اليدوية للـVIP | ❌ | `adminDashboard.controller.ts:176-178` | التعليق صريح: "no forced recompute … a true manual override" |

### القسم F — لوحة التحكم

| # | العنوان | الحالة | الدليل | ملاحظة |
|---|---|---|---|---|
| F1 | مدة المنتج ومنحه | ✅ | `adminProduct.controller.ts:136, 188` · `auth.controller.ts:112` | مدة بالأيام + منح للجميع + منح تلقائي عند التسجيل |
| F2 | إدارة المشرفين | 🟡 | `adminDashboard.controller.ts:1935-1960` · `admin-dashboard.html:688` | الكود والشاشة موجودان؛ حساب العميل على اللوحة أدمن عادي لا سوبر |
| F3 | رسائل الإدارة | ❌ | `broadcast.service.ts` تخص وقت البث لا الرسائل | لا endpoint لإرسال رسالة للجميع أو لمستخدم |
| F4 | حظر الجهاز/الشبكة (×2) | ❌ | لا موديل في `schema.prisma` | غير موجود إطلاقاً — شكوى العميل دقيقة |
| F5 | تسليم اللوحة الجديدة | ✅ | `backend/src/index.ts:150-161` | **نفس الرابط ونفس البيانات** — التفاصيل في القسم 5 |

### القسم G — الأداء والنشر والألعاب

| # | العنوان | الحالة | الدليل | ملاحظة |
|---|---|---|---|---|
| G1 | الأداء (×5) | ❌ | `app/android/app/build.gradle.kts:52-58` | APK 161MB بلا تصغير ولا تقسيم ABI |
| G2 | موقع تحميل الـAPK (×9) | ❌ | لا صفحة ولا سكربت رفع في الريبو | `beta.routes.ts` تخص تسجيل مختبِري Play فقط |
| G3 | الألعاب (×8) | 🟡 | `games_hub_screen.dart:19, 84` · `crash.service.ts:32` | 7 ألعاب ظاهرة، 4 مخفية، **هامش بيت 3% لا 0** |
| G4 | صور المتجر | ❓ | خارج الريبو | يُتحقق من Play Console |
| G5 | ظهور التطبيق في البحث | ❌ | `AndroidManifest.xml:34` `android:label="samafox"` | اسم إنجليزي صغير، لا ميتاداتا متجر في الريبو |
| G6 | النشر على Google Play | ❓ | `pubspec.yaml:4` `1.0.18+24` | لا ملف AAB في المخرجات؛ يُتحقق من Play Console |

---

## 3. البنود الناقصة — مرتبة بالأولوية

### 🔴 الأولوية القصوى

#### A2 — استقرار الصوت (×12، مفتوح لآخر يوم)

**الموجود:** WebRTC شبكة كاملة (mesh) — `Map<int, RTCPeerConnection>` في `webrtc_audio_service.dart:33`، اتصال واحد لكل مستخدم آخر. البنية ناضجة: إعادة تفاوض، polite/impolite، `_recoverPeer` عند الفشل، مهلة 8 ثوانٍ قبل إعادة البناء، مراقبة موت مسار المايك، AEC/NS/AGC مفعّلة في `_captureLocalStream`.

**الناقص — سببان مستقلان:**

1. **لا يوجد TURN.** `app/lib/config/app_config.dart:41` → `defaultValue: ''`، ولا سكربت بناء واحد في الريبو يمرّر `--dart-define=TURN_URLS`، ولا متغيّر TURN في `backend/.env`. النتيجة: كل نسخة release تعمل بـ STUN فقط. مستخدمان خلف CGNAT (وهو الوضع الطبيعي لمستخدمي بيانات الموبايل، أي غالبية جمهور التطبيق) لا يجدان مساراً مباشراً، فالصوت **لا يعمل من الأصل** أو ينهار عند أول تنقّل شبكة. تعليق الكود نفسه يسمّي هذا "السبب الأكبر لتقارير الصوت بيفصل على الهواتف" — وُثِّق ولم يُوصَّل.
2. **الطوبولوجيا mesh.** كل متحدث يرفع نسخة صوت لكل متحدث آخر. مع 8 مقاعد (`schema.prisma:171` الافتراضي) يرفع الهاتف 7 تدفقات. هذا مصدر "اللغوشة والتردد" في الغرف المزدحمة، ويناقض مطلب "استهلاك بيانات قليل" في G1 مناقضة مباشرة.

**أقل تغيير:**
- **فوري:** نصب coturn على نفس سيرفر AWS، وإضافة `--dart-define=TURN_URLS/TURN_USERNAME/TURN_CREDENTIAL` إلى أمر بناء الـrelease، وتثبيت الأمر في سكربت بناء داخل الريبو بدل تمريره يدوياً. هذا وحده يُتوقع أن يحل أغلب شكاوى الصوت.
- **متوسط المدى:** استبدال mesh بـ SFU (mediasoup أو LiveKit ذاتي الاستضافة، أو Agora كما اقترح العميل). SFU يجعل رفع الهاتف تدفقاً واحداً مهما كان عدد المتحدثين. الملفات: `webrtc_audio_service.dart` (طبقة النقل فقط) و`socket.service.ts` (معالجات `webrtc_*`).

#### A19 — تعيين مشرف الغرفة (مفتوح لآخر يوم)

**الموجود:** المسار كامل وسليم — `room_screen.dart:782` → `POST room-admin/add-admin` → `room-admin.controller.ts:117` → `roomMember.upsert`. الراوت مُركّب على `backend/src/index.ts:200`.

**الناقص — خطأ منطقي واحد:**
```
room-admin.controller.ts:78-79   const tier = await getPlatformTier(userId);
                                 if (tier) return tier;        // 'super' | 'platform'
room-admin.controller.ts:136-138 if (role === 'supervisor' && requesterRole !== 'owner'
                                                           && requesterRole !== 'admin')
                                   return 403
```
`getRoomRole` تفحص الرتبة المنصّية **قبل** فحص الملكية وتعود مبكراً. فالسوبر أدمن يحصل على `'super'` — وهي ليست `'owner'` ولا `'admin'` — فيُرفض. والأسوأ: **صاحب الغرفة نفسه لو كان سوبر أدمن يُرفض أيضاً**، لأن الفحص المنصّي يسبق `room.ownerId === userId`. العميل يختبر بحساب سوبر أدمن ⇒ "تعذر التعيين" في كل مرة.

**أقل تغيير:** في `addRoomAdmin` استبدال المقارنة النصّية بمقارنة رتبة عبر `ROLE_RANK` الموجود أصلاً في نفس الملف (`:64-71`) — أي اشتراط `ROLE_RANK[requesterRole] >= ROLE_RANK['admin']`، وهو ما يقبل `super` (5) و`platform` (4) و`owner` (3) و`admin` (2) تلقائياً. ملف واحد، دالة واحدة.

#### A17 — صلاحيات الأدمن والسوبر أدمن (×4)

**الموجود:** التنفيذ كامل على الطبقات الثلاث — مدد متدرّجة (`admin.controller.ts:265-280`)، حصانة الطاقم (`:305-310`)، طرد فوري من السوكت (`kickBannedUser`)، إبطال كاش الحظر، وواجهة كاملة في `room_screen.dart:1248-1330` بمدد يوم/شهر/أبدي للسوبر ويوم/يومين/3 للأدمن. حتى مشكلة الـverb عولجت (PUT وPATCH كلاهما مسجّل).

**الناقص:** ثلاث بوابات تفحص `isAdmin` وحده وتتجاهل `isSuperAdmin`:
- `backend/src/routes/admin.routes.ts:23` — `if (!user || !user.isAdmin) return 403` على **كل** مسارات `/api/v1/admin`
- `backend/src/middlewares/admin.middleware.ts:15` — نفس الفحص، `select: { id, isAdmin }`
- `app/lib/screens/room_screen.dart:1252` — `if (!(me?.userIsAdmin ?? false)) return;` ترجع بصمت

حساب سوبر أدمن بـ `isSuperAdmin=true` و`isAdmin=false` محجوب في الثلاثة معاً ⇒ الزر يظهر ولا يفعل شيئاً.

**أقل تغيير:** توسيع الفحوص الثلاثة إلى `isAdmin || isSuperAdmin`. وكحل فوري بلا نشر: ضبط `isAdmin=true` على حسابات السوبر أدمن في قاعدة البيانات (`backend/prisma/grant-admin.ts` موجود لهذا).

**بند فرعي منفصل:** `banUser` في `room-admin.controller.ts:228` لا يُصدر أي حدث سوكت لطرد المحظور فوراً (بعكس `muteUser` التي تُصدر `seat_mute_changed`) — يبقى المحظور في الغرفة حتى يخرج بنفسه، وهو ما يبدو للعميل كـ"غير فعّال".

#### A9 — إلغاء الصعود التلقائي

**الموجود:** `backend/src/services/socket.service.ts:1432` — `// ✅ Auto-seat admins` ثم `if (admins.has(uid))` يُجلس تلقائياً أول مقعد شاغر. و`admins` = مالك الغرفة + مشرفوها + **كل السوبر أدمن** (`:304, 315, 326`).

**الناقص:** الطلب (05/09) كان إلغاء السلوك كلياً ليتابع السوبر أدمن متخفياً. لم يُلمس.

**أقل تغيير:** حذف كتلة `1432-1508` بالكامل، مع الإبقاء على `emitRoomState` بعدها. لا تغيير في التطبيق مطلوب — زر المايك يعمل يدوياً أصلاً.

#### G1/G6 — حجم الحزمة والأداء (×5)

**الموجود:** تحسينات حقيقية نُفّذت — إطار بوستر بدل مشغّل فيديو لكل بلاطة في المتجر (`store_screen.dart:587`)، `CachedNetworkImage` بـ `memCacheWidth` (`app_network_image.dart:90`).

**الناقص — في إعدادات البناء:**
- `app/android/app/build.gradle.kts:52-58`: كتلة `release` بلا `isMinifyEnabled` ولا `isShrinkResources` (كلاهما معلَّق كتعليق)، بلا `splits { abi }`، بلا `abiFilters`.
- النتيجة على القرص: `app/build/app/outputs/flutter-apk/app-release.apk` = **161 ميجابايت** (05/09).
- لا ملف `.aab` في المخرجات إطلاقاً — وGoogle Play يشترط AAB وحدّه 150MB.
- `app/assets` = 50 ميجابايت خام، منها `mega/dragon.png` 2.5MB و`cards/card_aetherfall.png` 2.2MB و`aetherfall/bg_bonus_vault.png` 2.2MB.

**أقل تغيير:** تفعيل `isMinifyEnabled = true` و`isShrinkResources = true`، البناء بـ `flutter build appbundle` (Play يوزّع ABI واحد لكل جهاز تلقائياً)، وضغط أثقل 15 أصلاً إلى WebP. متوقع نزول الحجم إلى نطاق 40–60MB.

### 🟠 أولوية عالية

#### A3 — السماعة والإيربيس (×3)
**الموجود:** `setSpeakerphoneOn(bool)` (`webrtc_audio_service.dart:1106`) صحيحة، و`setRemoteVolume(0)` تُطفئ فعلاً عبر `track.enabled = false` (`:1092`) — **الشق الثاني من الطلب منفّذ ✅**.
**الناقص:** (أ) `_applyEchoSafeMode` (`:438-441`) تتجاهل وسيطها `talking` وتفرض `speakerOn: true` دائماً، وتُستدعى من 6 مواضع (`:309, 470, 504, 519, 1025, 1043`) — أي اختيار للإيربيس يُلغى عند أول كتم أو إعادة التقاط مايك. (ب) لا يوجد زر تبديل في `room_screen.dart` ينادي `setSpeakerphoneOn` أصلاً. (ج) صوت الألعاب عبر `audioplayers` مستقل تماماً (`games/plinko_sfx.dart:21`) بلا `AudioContext` مشترك.
**أقل تغيير:** جعل `_applyEchoSafeMode` تحترم حقل `_isSpeakerOn`؛ ربط أيقونة السماعة بـ `setSpeakerphoneOn`؛ وإنشاء موجّه صوت مشترك يضبط `AudioContext` لكل مشغّلات الألعاب عند تبديل المسار.

#### C16 — المتابعة والزوار
**الموجود:** API متابعة كامل — `backend/src/follow/follow.routes.ts:8-17` (متابِعون، متابَعون، طلبات، حالة، إلغاء).
**الناقص:** (أ) لا موديل `ProfileVisit` في `schema.prisma` ولا تتبّع زيارات إطلاقاً. (ب) لا تبويبات أصدقاء/أتابعه/يتابعني/الزوار في التطبيق — لا وجود لأي من هذه النصوص في `app/lib`. (ج) لا حساب متابعة متبادلة ⇒ "صديق". (د) لا بوابة ليفل من اللوحة.
**أقل تغيير:** موديل `ProfileVisit(viewerId, profileId, source, createdAt)` + كتابة صف عند فتح البروفايل من الرئيسية فقط + endpoint `/users/me/visitors` + إعداد `min_visitors_level` في `AppSetting` + شاشة تبويبات جديدة تستهلك `follow` الموجود.

#### C11 — نظام الشارات (×5)
**الموجود:** المنح التلقائي لكل درجة VIP/LV ✅ (`vip.service.ts:90-107` + `rewardItemIds`)، احترام الانتهاء ✅ (`user.controller.ts:522`)، الترتيب من الأقدم ✅ (`:527`)، المقاس يحكمه الكود ✅ (`vip_badge.dart:43`)، العرض في البروفايل وفي شيت المقعد داخل الغرفة ✅.
**الناقص — وهو بالضبط شكوى العميل القائمة:** `user.controller.ts:548` → `const data = [...badges, ...byType.values()]`. السطر يُلحق **ممثلاً واحداً عن كل نوع منتج آخر يملكه المستخدم** (إطار، مؤثر دخول، ثيم غرفة) بنفس صف الشارات. هذا حرفياً "الشارات مختلطة بمنتجات تانية".
**أقل تغيير:** إضافة `?onlyBadges=1` إلى `getUserBadges` وتمريرها من `_buildBadgesRow` — أو ببساطة حذف `...byType.values()` إن لم يعد لصف الأنواع مستهلك.

#### E3 — الترقية اليدوية للـVIP والليفل
**الموجود:** `adminUpdateUserProgression` (`adminDashboard.controller.ts:179`) تضبط `level` و`xp` و`vipLevel` كلاً على حدة، وتمنح مكافآت الدرجات المقطوعة عند الرفع ✅.
**الناقص:** التعليق في `:176-178` يعلنها قراراً تصميمياً: *"Each field is independently settable (no forced recompute between them) so this works as a true manual override"*. النتيجة: رفع `vipLevel` إلى 5 لا يمسّ `totalRecharge`، وتقدّم الـVIP يُحسب من `totalRecharge` (`vip.routes.ts:18`) ⇒ المستخدم يُطالَب بالـ6 مليون التراكمية من جديد. نفس الشيء لليفل: ضبط `level` بلا `xp` يُبقي `xp` منخفضاً.
**أقل تغيير:** عند ضبط `vipLevel` يدوياً، رفع `totalRecharge` إلى عتبة تلك الدرجة إن كان أقل. وعند ضبط `level`، رفع `xp` إلى `levelThresholdWithOverrides(level)` إن كان أقل. الدالتان موجودتان في `xp.service.ts:43` و`vip.service.ts`.

#### E2 — مصادر شحن الـVIP
**الموجود:** ثلاثة مسارات تزيد `totalRecharge` وتنادي `evaluateVip`: شحن الوكالة (`agency.controller.ts:236, 256`)، إضافة كوينز من `admin.controller` (`:68-69, 82`)، شحن وكالة الشحن (`charging-agency.controller.ts:391-392, 419`).
**الناقص:** مساران يزيدان `coinsBalance` فقط بلا `totalRecharge` وبلا `evaluateVip`:
- `adminDashboard.controller.ts:1276` — منح كوينز من لوحة التحكم ← **هذا بالضبط ما سمّاه العميل**
- `adminDashboard.controller.ts:453` — اعتماد طلب شحن وكالة

**أقل تغيير:** إضافة `totalRecharge: { increment: amount }` و`await evaluateVip(userId)` في الموضعين، على غرار `agency.controller.ts:236`.

#### A14 و A15 — إجمالي دعم الروم ونظام المكافآت
غير منفّذين إطلاقاً. الموجود قريب منهما ولا يغني عنهما: `AgencyChargeReward` (`agencyReward.service.ts`) تخص B14، و`supportersResetAt` تخص C15.
**أقل تغيير لـA14:** تجميع `giftTransaction` بـ `roomId` (نفس استعلام `buildSupportersBoard` بلا `groupBy senderId`) + إعداد `room_support_reset_hours` في `AppSetting` + اختصار k/m في `supporters_board_screen.dart`.
**أقل تغيير لـA15:** موديلان (`RoomCupReward`, `SupporterReward`) + قسم "المكافآت" في `admin-dashboard.html` + زر "مكافأة لك" في شاشة الكأس.

### 🟡 أولوية متوسطة

| البند | الموجود الآن | الناقص | أقل تغيير |
|---|---|---|---|
| **A1** الاحتفاظ بالغرفة | خدمة أمامية `microphone` مصرّحة وموصولة بقناة method | `POST_NOTIFICATIONS` غير مطلوب وقت التشغيل ⇒ الخدمة تفشل بصمت (`room_audio_keepalive.dart:44` تكتفي بـlog)؛ وإقرار Play Console لم يُملأ | طلب إذن الإشعارات عبر `permission_handler` قبل `start()`؛ ملء إقرار FOREGROUND_SERVICE في Play |
| **A8** أيقونة الموسيقى | الشريط مقصور على المشرفين (`music_player_bar.dart:66`) | لا `startedBy` في `buildMusicPayload`؛ الأيقونة العلوية (`room_screen.dart:2280`) للجميع | إضافة `startedBy` للحمولة والمقارنة بها في الموضعين |
| **A12** مكان الكأس | الكأس في الهيدر ✅، marquee داخل وخارج الغرفة ✅ | "إجراءات الغرفة" لم تُنقل داخل "إعدادات الغرفة" (التعليق في `:4139` يقر بذلك) | نقل عنصر القائمة بين الشيتين في `room_screen.dart` |
| **A16** غرفة 100000 | — | لا تثبيت ولا حجم كبير؛ `isFeatured` مُمرَّر `false` في كل مواضع `home_screen.dart` | `orderBy` مع تقديم 100000، وتمرير `isFeatured: room.id == 100000` |
| **B8** شاشة وكالتي | `listCharging/HostingAgencies` تُرجع الكل | لا فلترة لمالك الوكالة | فحص عضوية المالك في المتحكّم وإرجاع وكالته وحدها |
| **B6** تارجيت الشحن | الهدايا بقيمتها كاملة ✅ | `sendCoinsToUser` لا تزيد أي تارجيت | زيادة `targetAdjustmentCoins` للمُرسِل بمقدار `coins` (بلا عمولة) داخل نفس المعاملة |
| **B11** إجراءات التارجيت | معاملة ذرّية ✅ | `GLOBAL_DEFAULT_BLOCKED = true` — أي قاعدة بيانات بلا صف الإعداد تكون مقفولة كلياً، فتختفي الأزرار | ضبط `target_sell_blocked_global = 0` من اللوحة (بيانات لا كود) |
| **B13** تعديل التارجيت | يعمل على `agencyMember.id` | من ليس عضو وكالة لا صف له ⇒ "لا يعمل على السوبر أدمن" | إنشاء صف عضوية ضمني، أو تعديل عبر `userId` مع إنشاء عند الحاجة |
| **C6** فقاعة الخاص | الغرفة بـ9-slice صحيح ✅ | `bubbleUrl` يُرسَل في مسار الغرفة فقط (`socket.service.ts:1662`)؛ `message_bubble.dart:62` بـ`BoxDecoration` عادي | حمل `bubbleUrl` في حمولة الرسالة الخاصة وإعادة استخدام مُصيِّر الغرفة |
| **C17** مكان CP | المنطق كامل ✅ | `CpListScreen` مستورَد من `home_screen.dart` فقط | استدعاؤه من `profile_screen.dart` فوق قسم الهدايا الممنوحة |
| **C18** صور الخاص | الصوتيات ✅، الباك يقبل `imageUrl` ✅ | لا منتقي صور في `chat_screen.dart`، ولا بوابة VIP | منتقي صورة + رفع + `min_image_vip` في `AppSetting` يُفحص في `messages.controller` |
| **D6** شريط الهدية | فوق كل شيء ✅، مزخرف ✅، 1.5 ثانية ✅ | في المنتصف لا اليمين (`gift_announcement_bar.dart:124`) | استبدال `Positioned.fill` + margin بـ `Align(centerRight)` |
| **F2** إدارة المشرفين | الكود والشاشة موجودان ✅ | حساب العميل على اللوحة `isSuperAdmin=false` | ترقية حسابه عبر `backend/prisma/grant-admin.ts` — بيانات لا كود |
| **G3** الألعاب | 7 ألعاب حيّة، فنّ حقيقي، RTP مُثبت بمحاكاة | 4 ألعاب مخفية في `_hiddenGames`؛ لا قسم ألعاب في اللوحة؛ صوت الألعاب لا يتبع السماعة؛ **هامش البيت 3% لا 0** | إعادة الأربعة إلى `_games`؛ قسم لوحة للحدود والتشغيل؛ ربط الصوت بموجّه A3؛ **قرار العميل مطلوب على هامش البيت** |

### ⚫ لم يُبدأ فيه إطلاقاً

| البند | لا يوجد في الريبو | أقل تغيير |
|---|---|---|
| **A10** wakelock | لا حزمة، لا `WAKE_LOCK`، لا `keepScreenOn` | إضافة `wakelock_plus` وتفعيله في `main.dart` عند `resumed` |
| **A11** صوت التسجيل | لا `MediaProjection` ولا مسجّل شاشة داخل التطبيق | العميل يستخدم مسجّل النظام، وهو لا يلتقط تدفق `VOICE_COMMUNICATION` الذي يستعمله WebRTC. الحل: مسجّل داخلي بـ`MediaProjection` + `AudioPlaybackCapture` (يتطلب Android 10+)، أو توثيق القيد للعميل |
| **F3** رسائل الإدارة | لا endpoint إرسال للجميع/لمستخدم | endpoint في `adminDashboard` يكتب `Notification` جماعياً + قسم في اللوحة |
| **F4** حظر الجهاز/الشبكة (×2) | لا موديل، لا إنفاذ، لا واجهة | موديل `DeviceBan(deviceId, ip, reason, expiresAt)` + فحص في `auth.controller.login` + قسم لوحة. `deviceId` يُجمَع أصلاً في `auth_provider.dart:164` |
| **G2** صفحة تحميل APK (×9) | لا صفحة، لا سكربت رفع، لا استضافة APK | صفحة ثابتة في `backend/public/download.html` تخدم آخر APK من مجلد، أو رفع للاستضافة الحالية |
| **G5** ASO | `android:label="samafox"` إنجليزي صغير؛ لا مجلد ميتاداتا متجر | ضبط الاسم إلى "سما فوكس — غرف دردشة صوتية"، ومجلد fastlane metadata في الريبو |

---

## 4. الديون التقنية

### 4.1 الدين الأخطر: انفصال الفروع عن `main`

```
main               آخر كوميت 2026-07-04  (a4b8ecc)
greedy-cat-jackpot متقدّم بـ 81 كوميت
الفروع البعيدة     145 فرع
```
كل عمل أغسطس وسبتمبر — بما فيه دفعة الـ39 تعديل بالكامل — لم يُدمج في `main` قط. إن كان النشر يتم من `main` فالعميل لم يستلم شيئاً من هذه الدفعة، وهذا يفسّر منفرداً أغلب تكراراته بعد 26/08. **يجب التحقق من الفرع الذي بُني منه آخر APK وآخر نشر للباك إند قبل أي عمل آخر** — فقد يكون نصف "البنود الناقصة" أعلاه موجوداً في الكود ولم يصل الجهاز.

بالإضافة: 145 فرع بعيد أغلبها `codex/*` بأسماء فيتشرات (`add-mic-seat-lock`, `add-follow-button-states`, `add-relation-ring-to-gifts-dialog`…) — يحتمل أن بعض الفيتشرات المطلوبة مكتوبة على فرع لم يُدمج قط.

### 4.2 الإقرار الصريح في سجل الإصلاحات
`FIXES_2026-08-26.md` يختم بـ: *"Still nothing run against a live server, built for Android, or deployed"* — ويضيف أن `RoomAudioService.kt` **لم يُصرَّف قط** ("this machine has no Gradle cache"). أول APK release ظهر في 05/09، أي بعد 10 أيام من الدفعة. الثغرة الزمنية بين "الكود جاهز" و"العميل يجرّب" هي جوهر المشكلة.

كما ينبّه الملف إلى ثلاث ميجريشنات ذات عمل بيانات يجب أن تعمل **مرة واحدة بالضبط** (`20260823000000` تصالح تحويلات التارجيت التاريخية، `20260823020000` تملأ عدّاد الشحن الذاتي، `20260825000000` تزرع `gift_categories`). تشغيلها مرتين أو تخطّيها يفسد بيانات مالية. `deploy.sh` صُحِّح لهذا لكنه لم يُشغَّل بعد.

### 4.3 كود ميت
ملفات dart لا يستوردها أي ملف آخر:
```
app/lib/screens/achievements_screen.dart          app/lib/models/room_seat.dart
app/lib/screens/achievements_screen_new.dart      app/lib/models/room_state.dart
app/lib/screens/leaderboard_screen.dart           app/lib/models/room_activity_event.dart
app/lib/screens/test_api_screen.dart              app/lib/models/AvatarUploadResponse.dart
app/lib/widgets/chat_input_modal_fix.dart         app/lib/providers/room_admin_provider.dart
app/lib/gifts/widgets/medium_gift_lane.dart       app/lib/providers/wallet_provider.dart
app/lib/gifts/widgets/small_gift_stream_lane.dart app/lib/gifts/widgets/sender_banner_layer.dart
```
أخطرها **`providers/room_admin_provider.dart`**: دالتا `banUser` و`unbanUser` (`:199-226`) استدعاؤهما للـrepository **معلَّق كتعليق** والدالة تُرجع نجاحاً كاذباً. الملف ميت اليوم، لكن أي محاولة مستقبلية لاستعماله ستعطي "حظر" لا يفعل شيئاً — وهو نفس عرَض A17.

كذلك `achievements_screen.dart` و`achievements_screen_new.dart` يعرّفان **نفس اسم الكلاس** `AchievementsScreen`. وحقل `SeatsGrid.scrollable` (`seats_grid.dart:27`) لا يُمرَّر `true` أبداً.

### 4.4 تكرار
- `widgets/room_chat_panel.dart` و`widgets/room/room_chat_panel.dart` — نسختان، كلتاهما مستوردة من مكان ما، وكلتاهما معدَّلتان في الشجرة الحالية.
- `screens/leaderboard_screen.dart` و`screens/games/leaderboard_screen.dart`.
- `services/webrtc_service.dart` و`services/webrtc_audio_service.dart` — كلاهما يُعرِّف `setSpeakerphoneOn`؛ الأول يبدو ميتاً وظيفياً.
- `services/audio_service.dart` يُعرِّف `setSpeakerphoneOn` ثالثة (`:152`).

ثلاث نسخ من نفس دالة توجيه الصوت في مشروع شكواه الأولى هي توجيه الصوت — هذا سبب معماري مباشر لـA3.

### 4.5 حالات سباق ومسائل معمارية
- **مُعالَجة جيداً:** الخصم من المحفظة بـ`updateMany` + حارس `gte` في معاملة واحدة (`agency.controller.ts:215`) يمنع سباق القراءة-ثم-الكتابة. `music_ended` محمي بحارس `index` ونافذة 1.5 ثانية. تحويل/بيع التارجيت في `$transaction` ذرّية.
- **`_readLocalAudioLevel`** (`webrtc_audio_service.dart:1437`) تبدأ بـ`if (_peerConnections.isEmpty) return null;` وتقرأ `values.first` فقط ⇒ **مستخدم وحده في الغرفة لا تظهر له دائرة تكلّم إطلاقاً**، وإذا كان أول اتصال ميتاً تتوقف القراءة.
- **الاعتماد على `?? 0`** في فحوص الرتب (`ROLE_RANK[requesterRole] ?? 0`) يجعل أي رتبة جديدة تفشل بصمت إلى "لا صلاحية" بدل الانفجار — سيّئ للتشخيص، وهو نمط الخطأ نفسه الذي أنتج A19.
- **`usesCleartextTraffic="true"`** (`AndroidManifest.xml:37`) مع باك إند على `http://63.179.163.62:3000` — كل التوكنات والرسائل تسير بلا تشفير. دين أمني قائم ونقطة مراجعة محتملة في Play.
- **مفاتيح خاصة داخل الريبو:** `NewPrivateKey.ppk`, `SamaFox-NewServerKey-PrivateKey.ppk`, `samafoxkey.key`, و`backend/.env` — يجب نقلها خارج التتبّع وتدوير المفاتيح.

### 4.6 فيتشرز في الكود بدون طلب مكتوب
- **11 لعبة** بينما الشات يذكر 3 مختارة + مشتراة: بلينكو، عجلة الحظ، طيّار، أثيرفول، القط الجشع، أستيريون، نيون فورتشن (حيّة) — وعجلة المهارة، صياد السمك، الأسد والنمر، نرد المهارة (مخفية). مع محاكيات إثبات RTP (`npm run sim:*`) وبذور provably-fair ثابتة.
- **موديول تلعيب كامل**: `quest.controller.ts`, `achievement.controller.ts`, `daily_quests_screen.dart`, `challenges_screen.dart`.
- **`beta.routes.ts` / `betaWatchdog.service.ts`** — تسجيل مختبِري Google Play عبر البريد.
- **`analytics_screen.dart`, `explore_screen.dart`, `premium_screen.dart`, `broadcast_time_screen.dart`**.
- **`search/`** و**`relations/relationRing.service.ts`**.
- **ملاحظة تنقّل:** تبويبا الألعاب ووكيل الشحن **معلَّقان** في الشريط السفلي (`main_navigation_screen.dart:86-87`) — الشريط تبويبان فقط. الشاشتان تُفتحان من داخل الغرفة ومن "وكالاتي".

---

## 5. البنود المحتاجة تأكيداً يدوياً

| البند | لماذا لا يُحكم عليه من الكود | كيف تتأكد |
|---|---|---|
| **F5** رابط اللوحة | إجابة قاطعة من الكود، لكن التطبيق الفعلي يعتمد على النشر | **الجواب:** لوحة واحدة فقط — `backend/public/admin-dashboard.html` تُخدَم ثابتة من `index.ts:150-161`. **نفس الرابط:** `http://63.179.163.62:3000/admin-dashboard.html` — لا رابط جديد ولا بيانات دخول جديدة. الدخول بإيميل وباسورد حساب التطبيق نفسه عبر `/auth/login` (`admin-dashboard-auth.routes.ts:32`)، والوصول يشترط `isSuperAdmin` (`adminDashboard.middleware.ts:31`). "اللوحة الجديدة" = نفس الملف بإضافات 26/08 (كوميت `3a7638d`). **لن يراها العميل حتى يُنشر فرع `greedy-cat-jackpot` على السيرفر.** تحقق: افتح الرابط وابحث عن قسم "🎁 مكافآت الشحن التلقائية" (`admin-dashboard.html:364`) — وجوده يعني أن النشر تم |
| **A2** الصوت | يعتمد على شبكة حقيقية وأجهزة حقيقية | جهازان على شبكتي بيانات مختلفتين (لا واي فاي واحد) في نفس الغرفة. راقب `❄️ ICE connection state` في اللوج: ثبات على `checking` أو `failed` = تأكيد غياب TURN |
| **A11** صوت التسجيل | يعتمد على مسجّل النظام ونسخة أندرويد | سجّل الشاشة بالمسجّل المدمج أثناء غرفة نشطة. الصوت غائب = تأكيد أن `VOICE_COMMUNICATION` لا يُلتقط، ولا حل بلا مسجّل داخلي |
| **D1** هدايا الفيديو | فشل خاص بجهاز/كودك | جرّب على الهاتف الذي فشل معه واقرأ لوج `video_player`. الأرجح كودك غير مدعوم لا إذن — أعد ترميز الفيديوهات إلى H.264 baseline |
| **B7** تارجيت الشحن مع الاستضافة | يعتمد على وجود بيانات فعلية | أنشئ وكيل شحن، سجّل له مضيفين، وافحص ظهور تارجيته في قسم الوكالات باللوحة |
| **D2** قائمتا CP و VIP | البنية موجودة، الصفوف في قاعدة البيانات | `SELECT * FROM gift_categories;` وابحث عن CP و VIP |
| **C10** صورة الغرفة المتحركة | البوابة موجودة للأفاتار | ارفع GIF كصورة غرفة بحساب VIP منخفض وتحقق من الرفض |
| **G4** صور المتجر | خارج الريبو | Play Console ← Store listing ← Graphics، وقارن بالصور المرسلة 05/09 |
| **G6** حالة النشر | خارج الريبو | Play Console ← Release overview. تحقق أيضاً من أي مخالفات مفتوحة، وخصوصاً إقرار `FOREGROUND_SERVICE_MICROPHONE` — حساب المطوّر نفسه رُفض له تطبيق سابق لهذا السبب |
| **G2** صفحة التحميل | خارج الريبو | افتح `https://almobarmg.com/samafox` وتحقق من أن الملف المخدوم هو `1.0.18+24` |

---

## 6. خطة التنفيذ المقترحة

### الموجة 1 — عاجل (3–5 أيام)
**الهدف: أن يرى العميل تغيّراً حقيقياً في اليوم الأول.**

| العمل | الوقت |
|---|---|
| **تحديد ما هو منشور فعلاً**: أي فرع بُني منه APK 05/09، وأي كوميت يعمل على السيرفر. قبل هذا كل تقدير آخر مبني على فراغ | 2 س |
| دمج `greedy-cat-jackpot` في `main`، نشر الباك إند بـ`deploy.sh` (**نسخة احتياطية لقاعدة البيانات أولاً** — 3 ميجريشنات ذات عمل بيانات) | 4 س |
| **نصب coturn وتوصيل `TURN_URLS`** في أمر البناء ← A2 | 6 س |
| إصلاح `addRoomAdmin` بمقارنة `ROLE_RANK` ← A19 | 30 د |
| توسيع فحوص `isAdmin` إلى `isAdmin \|\| isSuperAdmin` في 3 مواضع + ترقية حساب العميل ← A17, F2 | 2 س |
| حذف كتلة الصعود التلقائي ← A9 | 30 د |
| إصدار حدث سوكت لطرد المحظور فوراً ← A17 | 1 س |
| `target_sell_blocked_global = 0` من اللوحة ← B11 | 10 د |
| تفعيل minify/shrink وبناء AAB ← G1, G6 | 3 س |
| **إجمالي** | **~3 أيام عمل** |

### الموجة 2 — مهم (7–10 أيام)

| العمل | الوقت |
|---|---|
| توجيه صوت موحّد: احترام `_isSpeakerOn`، ربط زر السماعة، `AudioContext` مشترك للألعاب ← A3, G3(هـ) | 2 ي |
| wakelock ← A10 | 2 س |
| `evaluateVip` + `totalRecharge` في مساري اللوحة ← E2 | 2 س |
| مزامنة `xp`/`totalRecharge` مع الترقية اليدوية ← E3 | 3 س |
| فصل الشارات عن باقي المنتجات ← C11 | 2 س |
| إجمالي دعم الروم + تصفير من اللوحة ← A14 | 1.5 ي |
| نظام المكافآت (كأس الروم + الداعمين) ← A15 | 2 ي |
| فقاعة الخاص + منتقي الصور + بوابة VIP ← C6, C18 | 1.5 ي |
| نقل CP إلى البروفايل ← C17 | 3 س |
| فلترة "وكالتي" ← B8؛ تارجيت الشحن ← B6؛ تعديل تارجيت السوبر أدمن ← B13 | 1 ي |
| صفحة تحميل APK ← G2 | 4 س |
| **إجمالي** | **~9 أيام عمل** |

### الموجة 3 — تحسينات (10–15 يوماً)

| العمل | الوقت |
|---|---|
| **الانتقال من mesh إلى SFU** (mediasoup/LiveKit/Agora) ← A2 جذرياً + G1 | 5 ي |
| نظام الزوار والتبويبات الأربعة ← C16 | 2.5 ي |
| حظر الجهاز/الشبكة ← F4 | 1.5 ي |
| رسائل الإدارة ← F3 | 1 ي |
| قسم الألعاب في اللوحة + إعادة الأربعة المخفية ← G3(د)(أ) | 2 ي |
| مسجّل شاشة داخلي بالصوت ← A11 | 2 ي |
| ترتيب الغرف وتثبيت 100000 ← A16؛ نقل إجراءات الغرفة ← A12 | 4 س |
| تنظيف الكود الميت والملفات المكرّرة (§4.3، §4.4) | 1 ي |
| نقل المفاتيح خارج التتبّع وتدويرها + الانتقال إلى HTTPS | 1 ي |
| خطة ASO وميتاداتا المتجر ← G5 | 1 ي |
| **إجمالي** | **~17 يوم عمل** |

---

## 7. الخلاصة

الفريق كتب كوداً جيداً — أفضل من أن يستحق حجم الشكاوى. أنظمة الوكالات والتارجيت والهدايا والشارات وضبط الإطارات منفّذة بعناية حقيقية، بمعاملات ذرّية وحراسات سباق وتعليقات تشرح سبب كل قرار.

لكن ثلاثة أشياء أفسدت الصورة:

1. **الشحن.** كود صحيح على فرع غير مدموج لا يساوي شيئاً عند العميل. `main` متجمّد منذ يوليو، وسجل الإصلاحات نفسه يقر بأن دفعة الـ39 لم تُبنَ ولم تُنشر يوم كتابتها.
2. **بند واحد لم يُوصَّل.** غياب TURN وحده قادر على إنتاج كل شكاوى الصوت الاثنتي عشرة — والكود يعرف ذلك ويقوله في تعليقه.
3. **فحوص صلاحيات ضيّقة.** `isAdmin` وحده و`requesterRole !== 'owner'` يشلّان حساب السوبر أدمن — وهو الحساب الوحيد الذي يختبر به العميل. فيتشرات كاملة تبدو "غير فعّالة" لأن من يجرّبها محجوب عنها.

**إصلاح هذه الثلاثة — نشرة واحدة، وسيرفر TURN، وثلاثة أسطر شروط — يُتوقع أن يغلق أو يخفّف أكثر من نصف الشكاوى المفتوحة قبل كتابة أي فيتشر جديدة.**
