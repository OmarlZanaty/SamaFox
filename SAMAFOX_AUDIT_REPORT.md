# تدقيق تقني كامل — SamaFox

### طلبات العميل من 01/08/2026 إلى 10/09/2026

**تاريخ التدقيق:** 11/09/2026 · **الفرع المفحوص:** `audit-remaining-items` (`0dd671d`)
**النطاق:** تطبيق Flutter · باك إند Node/TypeScript · لوحة تحكم HTML/JS ثابتة
**عدد البنود:** 77 · **الطريقة:** ستة فاحصين متوازيين، كل حكم مربوط بـ `ملف:سطر`

> هذا التدقيق **لا يثق** بـ `SAMAFOX_DELIVERY.md`. ذلك الملف *ادّعاء* بأن كل شيء
> أُنجز؛ مهمة هذا التقرير التحقق منه. وفي بندين على الأقل (A2، G4) الادّعاء غير صحيح.

---

## 1. الملخص التنفيذي

**37 ✅ منفّذ (48%) · 33 🟡 جزئي (43%) · 5 ❌ لم يُنفّذ (6%) · 2 ❓ (3%).**

الكود أفضل بكثير مما توحي به شكاوى العميل، والسبب الجذري لتكراره البنود **ليس** أن
الكود لم يُكتب — بل أنه **لم يصل إليه**. حتى لحظة هذا التدقيق كان `origin/main` عند
04/09، وكل الشغل الذي يغلق شكاوى العميل (11 كوميت بين 05/09 و10/09) كان على فرع غير
مدموج. أثبتُّ ذلك حيًّا: `asterion` و`olympus` يرجّعان 404 على سيرفر الإنتاج بينما
`aetherfall` و`greedy` يرجّعان 401 — أي أن الإنتاج يشغّل `origin/main` بالضبط.

**تم الدمج في `main` أثناء هذا التدقيق** (`6cf571f`، 40 كوميت فوق `origin/main`).
الدمج احتوى **10 ملفات متعارضة**، أحدها (`games_hub_screen.dart`) كان سيُنتج
`case` مكرّرة خمس مرات — أي خطأ ترجمة يمنع البناء أصلًا. بعد الحل: 12 لعبة ↔ 12
`case`، و**صفر أخطاء وصفر تحذيرات** في `flutter analyze`.

**أخطر 5 فجوات:**

1. **A2 — الصوت (×12): لم يُغلق.** أربعة أعطال مستقلة، كل واحد كافٍ وحده: (أ) لا يوجد
   سيرفر TURN مُهيّأ — `turn.env` غير موجود ولا سكربت coturn في أي مكان، فكل نسخة
   تعمل STUN فقط؛ (ب) **كل** عضو في الغرفة مايك مفتوح في شبكة mesh كاملة
   (`webrtc_audio_service.dart:532` سطر الكتم *معطّل بتعليق*) — 30 شخصًا = 30 مايك
   يبثّ؛ (ج) السوكيت يستسلم نهائيًا بعد 5 محاولات ولا أحد يعيد الاتصال؛
   (د) زر "تقليل الضوضاء" لا يلمس مسار الصوت إطلاقًا.
2. **G3(c) — "المالك لا يكسب ولا يخسر": غير منفّذ إطلاقًا.** الألعاب الاثنتا عشرة كلها
   *مصرفية* (المالك هو الطرف المقابل في كل رهان). ألعاب الكازينو تأخذ 1–3%، وألعاب
   "المهارة" الأربع تأخذ **45–60%**، وثلاثة ملفات تقول ذلك حرفيًا:
   `boxing.service.ts:25` «the platform stays net-positive».
3. **A17 — الحظر: الخلل في التطبيق لا في السيرفر.** `_$UserToJson` لا يُسلسِل
   `isSuperAdmin`، وقراءة `localStorage` حيًّا أكّدت غيابه من نسخة المستخدم
   المخزَّنة. لكن الاختبار الحيّ أظهر «إغلاق الغرفة» بعد إعادة تحميل كاملة،
   فالأثر العملي **غير مؤكَّد** — راجع §9. الفقد حقيقي والإصلاح سطر واحد.
4. **D4 — حذف المنتج: سطر واحد خاطئ.** `app.js:1474` ينادي `/admin/products`
   (حذف بلا cascade ⇒ 500) بدل `/admin-products/products`. المعالج الصحيح مكتوب
   ومسجّل ولا يُنادى عليه أبدًا.
5. **G4 — صور المتجر: لم تصل.** الصور الأربع المرسلة في 05/09 **غير موجودة في أي
   كوميت**. والأسوأ، شوهد حيًّا: `Ads.png` نفسها **صورة تصميم** فيها ثلاثة إعلانات
   وهمية ونقاط كاروسيل مرسومة داخلها، تُعرض ثلاث مرات (`home_screen.dart:766`).

| الموديول | الحالة | ملاحظة |
|---|---|---|
| الوكالات والتارجيت (B) | 🟢 10/15 ✅ | أنظف كود في المشروع؛ الثغرات في نقاط اتصال لا في الحساب |
| المظهر والبروفايل (C) | 🟢 12/18 ✅ | الناقص كله تجميعي صغير في صفحة البروفايل |
| الغرف والصلاحيات (A12–A21) | 🟡 5/10 ✅ | A17 أولوية قصوى، A18 غير منفّذ |
| الصوت (A1–A11) | 🔴 6/11 ✅ | A2 ما زال مفتوحًا بالكامل |
| الهدايا والإشعارات (D) | 🟡 1/9 ✅ | معظمها فروق ترتيب طبقات (z-order) |
| الليفل والـVIP (E) | 🟡 1/3 ✅ | E1 لم يعد يرتفع بلا دعم، لكن جدول اللوحة غير مُلزِم |
| لوحة التحكم (F) | 🟡 2/5 ✅ | F4 مبني بالكامل لكن التطبيق لا يرسل معرّف الجهاز |
| الأداء والنشر والألعاب (G) | 🔴 0/6 ✅ | G3 يحتاج إعادة تصميم اقتصادي لا ترقيعًا |

---

## 2. الجدول الرئيسي

| # | العنوان | الحكم | الدليل | ملاحظة |
|---|---|---|---|---|
| A1 | ثبات المستخدم في الغرفة | 🟡 | `RoomAudioService.kt:88`, `room_screen.dart:4834` | خدمة أمامية حقيقية لكنها تُشغَّل عند أخذ المايك فقط |
| A2 | استقرار الصوت (×12) | ❌ | `app_config.dart:39`, `webrtc_audio_service.dart:532`, `app_config.dart:62` | لا TURN، مايك مفتوح للجميع، السوكيت يموت بعد 5 محاولات |
| A3 | السماعة (×3) | 🟡 | `audio_route.dart:35`, `room_screen.dart:2142` | يعمل للصوت والألعاب؛ المؤشر ليس على شريط الغرفة، والسلايدر لا يطال الألعاب |
| A4 | دائرة المتكلم | ✅ | `seat_card.dart:94`, `webrtc_audio_service.dart:1466` | VAD حقيقي بعتبة 0.02 ودورة كاملة عبر السوكيت |
| A5 | حد المايكات | 🟡 | `room_screen.dart:4130`, `seats_grid.dart:27` | الصندوق نصف الشاشة لكنه تحت الهيدر؛ `scrollable` كود ميت |
| A6 | المايك الكامل/جميع الغرفة | ✅ | `gifts/controller.ts:451`, `gift_picker_sheet.dart:363` | الخصم المجمّع صحيح رياضيًا |
| A7 | نظام الموسيقى | ✅ | `music.controller.ts:46`, `socket.service.ts:886` | الصلاحية مفروضة على السيرفر لا في الواجهة فقط |
| A8 | أيقونة الموسيقى | ✅ | `music_player_bar.dart:81` | الشريط للمشغّل، الصوت للجميع |
| A9 | إلغاء الصعود التلقائي | ✅ | `socket.service.ts:1432` | الكتلة محذوفة والسبب موثّق مكانها |
| A10 | منع إطفاء الشاشة | ✅ | `main.dart:92`, `main.dart:299` | على مستوى التطبيق، يُفك عند الخلفية |
| A11 | صوت التسجيل | 🟡 | `ScreenRecordService.kt:203` | مسار الصوت مضبوط لكنه ينازع WebRTC على المايك |
| A12 | مكان الكأس + marquee | ✅ | `room_screen.dart:4270`, `marquee_text.dart:116` | قياس حقيقي قبل التمرير |
| A13 | الكأس يتفاعل مع الدعم (×4) | ✅ | `gifts/controller.ts:284`, `supporters_board_screen.dart:76` | 30 للبرنامج و20 للغرفة، والقيمة تحت المايك فورية |
| A14 | إجمالي دعم الروم (×3) | ✅ | `roomReward.service.ts:45`, `supporters_board_screen.dart:245` | نافذة متحركة (لا cron يمكن أن يفشل) + k/m |
| A15 | نظام المكافآت | 🟡 | `roomReward.service.ts:85`, `supporters_board_screen.dart:260` | المنطق كامل؛ زر «مكافأة لك» فوق القائمة لا جنب الاسم |
| A16 | غرفة الإدارة | 🟡 | `room.controller.ts:12`, `home_screen.dart:445` | التثبيت يعمل؛ الكارت القديم الميت ما زال معروضًا |
| A17 | صلاحيات الأدمن (×4) | 🟡 | `user.g.dart:57`, `admin.controller.ts:265` | السيرفر سليم؛ الكاش يفقد `isSuperAdmin` (مؤكَّد من localStorage) والأثر العملي غير حاسم — §9 |
| A18 | صلاحيات مايك للأدمن | ❌ | `socket.service.ts:321`, `room_screen.dart:5740` | أدمن المنصة غير مُدرَج في `adminIds` أصلًا |
| A19 | تعيين المشرف | ✅ | `room-admin.controller.ts:142` | اتُصلح بالرتبة `rankOf()` بدل مقارنة نصية |
| A20 | فتح البروفايل من التيكست (×3) | 🟡 | `room_screen.dart:5080`, `room_screen.dart:5433` | يفتح، لكن يعرض LV 0 وبدون VIP وبالأيدي الداخلي |
| A21 | فتح البروفايل من الرسائل | ✅ | `chat_screen.dart:309` | الصورة والهيدر والقائمة كلها تفتح |
| B1 | عمولة الوكيل للتارجيت | ✅ | `giftService.ts:199` | لا كتابة على `coinsBalance` في كتلة العمولة إطلاقًا |
| B2 | هدية الوكيل على نفسه (×3) | ✅ | `giftService.ts:93`, `agency.controller.ts:1315` | مثال العميل يتحقق: 1M خصم، 1.2M تارجيت |
| B3 | شرط صرف العمولة | ✅ | `agency.controller.ts:1457` | القفل مفروض عند الصرف |
| B4 | مصدر كوينزات وكالة الشحن (×2) | 🟡 | `agency.controller.ts:242`, `charging-agency.controller.ts:648` | المحفظة الشخصية تعمل؛ الرصيد المنفصل لم يُحذف وما زال قابلًا للكتابة |
| B5 | فروع وكالة الشحن (×4) | 🟡 | `agency.controller.ts:207`, `charging-agency.controller.ts:458` | الفرع يبيع من محفظته؛ لكنه لا يستطيع طلب شحن لنفسه (403) |
| B6 | تارجيت وكيل الشحن | 🟡 | `agency.controller.ts:266`, `charging-agency.controller.ts:349` | المسار المستخدم صحيح؛ مسار ثانٍ لا ينزّل تارجيت |
| B7 | تارجيت وكيل الشحن في اللوحة | 🟡 | `adminDashboard.controller.ts:1004`, `admin-dashboard.html:357` | الـAPI يحسبه؛ لا يوجد عمود في الجدول |
| B8 | شاشة وكالتي | ✅ | `agency.controller.ts:78` | فلتر على السيرفر + تعزيز في التطبيق |
| B9 | بيع وتبديل التارجيت (×3) | ✅ | `agency.controller.ts:1814`, `agency.controller.ts:1735` | متاح للمضيف والوكيل، بلا حد أقصى، والتبديل نصف القيمة |
| B10 | منع البيع لمستخدم محدد (×2) | ✅ | `adminDashboard.controller.ts:774`, `targetLock.ts:70` | يقبل الأيدي الظاهر الآن |
| B11 | اختفاء إجراءات التارجيت | 🟡 | `settings_screen.dart:173`, `agency.controller.ts:1716` | عادت للواجهة؛ لكن فحص الرصيد خارج المعاملة (TOCTOU) |
| B12 | تارجيت غير المسجّل (×2) | ✅ | `agency.controller.ts:1606`, `profile_screen.dart:1255` | مشتق من عضوية حقيقية فقط |
| B13 | تعديل التارجيت من اللوحة (×2) | ✅ | `adminDashboard.controller.ts:2129` | يحلّ بالأيدي الظاهر ثم الداخلي فيصل للسوبر أدمن |
| B14 | تتبع الشحنات (×2) | ✅ | `agencyReward.service.ts:47`, `adminDashboard.controller.ts:2631` | العدّاد والبحث وسلّم المكافآت الثلاثة موجودة |
| B15 | إزالة مضيف بلا رسوم | ✅ | `agency.controller.ts:790` | مسار منفصل عن `leaveAgency` |
| C1 | الجنس والعمر (×2) | ✅ | `profile_screen.dart:1163`, `room_screen.dart:5362` | في الصفحة وفي كارت الغرفة |
| C2 | ترتيب بيانات البروفايل (×3) | 🟡 | `profile_screen.dart:1190`, `agency.controller.ts:1132` | كارت الغرفة مضبوط؛ صفحة البروفايل لا، ولا يوجد «وكيل شحن» ولا «وكيل س، ش» |
| C3 | شكل الليفل والـVIP (×4) | 🟡 | `vip_badge.dart:62`, `room_chat_panel.dart:322` | مستطيلات مزخرفة حقيقية؛ سطر الشات يستخدم شريحة مسطّحة محلية |
| C4 | شريط الدخولية (×4) | ✅ | `entrance_banner_layer.dart:237`, `:203` | الكلام داخل اللوحة والارتفاع من المحتوى |
| C5 | مركبة الدخول (×2) | ✅ | `room_screen.dart:4708` | انتقلت فوق المقاعد |
| C6 | فقاعات الدردشة (×3) | ✅ | `bubble_skin.dart:138`, `messages.controller.ts:202` | تكبر مع النص، وتعمل في الخاص، وتختفي بانتهاء المدة |
| C7 | ضبط الإطارات (×5) | ✅ | `FramedAvatar.dart:95`, `:318` | الكود يقيس البكسل غير الشفاف ويحكم المقاس |
| C8 | خلفية البروفايل (×3) | ✅ | `profile_screen.dart:1099`, `user.controller.ts:26` | في بروفايل المستخدم الذي يفتح من الغرفة |
| C9 | إطار تزيين الصفحة (×2) | ✅ | `profile_screen.dart:1331` | آخر طبقة، `IgnorePointer`، لا يغطي بيانات |
| C10 | الصورة المتحركة (×3) | 🟡 | `upload.controller.ts:205`, `:14` | البروفايل محمي بالـVIP؛ صورة الغرفة بلا أي فحص |
| C11 | نظام الشارات (×5) | 🟡 | `vip.service.ts:66`, `socket.service.ts:496`, `profile_screen.dart:885` | لا backfill، والغرفة تعرض *إنجازات* لا شارات، وأيقونة الشارات لم تُحذف |
| C12 | إظهار الـVIP مع الشارات | 🟡 | `room_screen.dart:5432`, `profile_screen.dart:2067` | الغرفة صحيحة؛ صفحة البروفايل بلا شريحة VIP (`_buildVIPCard` كود ميت) |
| C13 | تنسيق المتجر | ✅ | `store_screen.dart:171` | مسافات 10/16؛ صف البروفايل وحده بلا فواصل |
| C14 | مدة المنتج عند الشراء | ✅ | `store_screen.dart:650`, `:685` | على البلاطة وفي نافذة التأكيد |
| C15 | أعلى المستويات (×2) | ✅ | `supporters_board_screen.dart:184`, `adminDashboard.controller.ts:2343` | التصفير غير دائم ويؤثر على لوحة التطبيق نفسها |
| C16 | المتابعة والزوار | 🟡 | `relations_screen.dart:121`, `relations.controller.ts:130` | التبويبات الأربعة حقيقية؛ صف البروفايل ما زال يكرّر Level/VIP |
| C17 | نظام CP (×3) | ✅ | `cp.service.ts:181`, `profile_screen.dart:1276` | 30% عند الرفض، وانتقل فوق الهدايا الممنوحة |
| C18 | صور وصوتيات الخاص | ✅ | `chat_screen.dart:608`, `messages.controller.ts:243` | بوابة VIP مفروضة على السيرفر |
| D1 | هدايا الفيديو (×5) | 🟡 | `video_gift_player.dart:72`, `gift_animation_overlay.dart:325` | الصوت مفعّل؛ لكن التوجيه الصوتي غير مسجّل و`onError` يبتلع الفشل بصمت |
| D2 | تعريب القوائم + CP وVIP | ✅ | `migration.sql:48`, `gift_picker_sheet.dart:60` | 11 قائمة عربية، وCP/VIP تظهران دائمًا |
| D3 | إدارة القوائم من اللوحة (×4) | 🟡 | `adminDashboard.controller.ts:1575`, `app.js:1620` | الباك كامل؛ اللوحة المستخدمة تفرض `category:'admin'` ولا زر نقل |
| D4 | حذف المنتج من الطرفين (×3) | ❌ | `app.js:1474`, `adminProduct.controller.ts:367` | **رابط خاطئ — المعالج الصحيح موجود ولا يُنادى** |
| D5 | كل المنتجات صورة أو فيديو (×2) | 🟡 | `adminProduct.controller.ts:81`, `FramedAvatar.dart:173` | الرفع يقبل الاثنين؛ الإطار والفقاعة والدخولية لا ترسم فيديو |
| D6 | شريط الهدية | 🟡 | `gift_announcement_bar.dart:111`, `main.dart:399` | يمين ✅ ومزخرف ✅؛ لم يُرفع، وليس الأعلى طبقةً |
| D7 | شريط الدخولية يغطي | 🟡 | `room_screen.dart:4747` | ارتفع فوق المقاعد لكن ثلاث طبقات فوقه |
| D8 | مدة ومكان الإشعارات | 🟡 | `global_notification_service.dart:44`, `main.dart:205` | 1.5 ثانية ✅ والقائمة ✅؛ إشعار الهدية ما زال فوق الشريط |
| D9 | شريط معرّف الهدية | 🟡 | `gift_socket_service.dart:118` | النص و«إلى الجميع» صحيحان؛ الموضع يمين لا نص (D6 وD9 على ودجت واحدة) |
| E1 | الليفل لا يتفاعل (×5) | 🟡 | `giftService.ts:253`, `xp.service.ts:46` | **لا يوجد أي مسار يرفع الليفل بلا دعم**؛ لكن جدول اللوحة غير مُلزِم |
| E2 | مصادر شحن الـVIP | ✅ | `agency.controller.ts:273`, `admin.controller.ts:67` | كل المسارات الأربعة تزيد `totalRecharge` وتنادي `evaluateVip` |
| E3 | الترقية اليدوية | 🟡 | `adminDashboard.controller.ts:231`, `vip.routes.ts:97` | اللوحة صحيحة؛ **شراء VIP يعيد نفس العطل** |
| F1 | مدة المنتج ومنحه | ✅ | `adminProduct.controller.ts:136`, `auto-grant.service.ts:12` | المنح للجميع يشمل المسجّلين الجدد بنفس المدة |
| F2 | إدارة المشرفين | 🟡 | `adminDashboard.middleware.ts:31`, `admin-dashboard.html:708` | السيرفر صحيح؛ زر التعيين يظهر للأدمن العادي فيأخذ 403 |
| F3 | رسائل الإدارة | ✅ | `adminDashboard.controller.ts:2517`, `:2538` | للجميع أو بالأيدي الظاهر |
| F4 | حظر الجهاز/الشبكة (×2) | 🟡 | `deviceBan.middleware.ts:29`, `dio_client.dart` | مبني بالكامل؛ **التطبيق لا يرسل `x-device-id` ولا يُحفظ جهاز أو IP** |
| F5 | تسليم اللوحة الجديدة | ❓ | `index.ts:151` | لا توجد لوحة ثانية — نفس الرابط ونفس البيانات؛ يلزم النشر للتأكد |
| G1 | الأداء (×5) | 🟡 | `store_screen.dart:598`, `build.gradle.kts:65` | النقاط الساخنة اتصلحت؛ 61MB أصول و APK موحّد لكل المعماريات |
| G2 | موقع التحميل (×9) | 🟡 | `appDownload.routes.ts:76`, `public/downloads/` | الصفحة والمسار جاهزان؛ **لا APK مرفوع** |
| G3 | الألعاب (×8) | ❌ | `boxing.service.ts:25`, `skillDice.service.ts:24` | **كل الألعاب مصرفية؛ ألعاب المهارة تأخذ 45–60%** |
| G4 | صور المتجر | ❌ | `home_screen.dart:766` | الصور غير موجودة في أي كوميت؛ السلايدر يكرر صورة واحدة |
| G5 | ASO | 🟡 | `docs/aso-plan.md:1`, `values-ar/strings.xml` | كل ما في الريبو أُنجز؛ الباقي حقول Play Console |
| G6 | النشر على Play | ❓ | `beta.service.ts:5`, `pubspec.yaml:4` | اختبار **مغلق** لا مفتوح؛ حالة الإصدار غير مرئية من الكود |

---

## 3. البنود الناقصة مرتبة بالأولوية

### أولوية قصوى

**A2 — الصوت (×12)**
- **الموجود:** طبقة تفاوض ممتازة فعلًا — تخزين مرشحات ICE المعلّقة، تفاوض مهذّب مع
  rollback، استرجاع متدرّج ICE-restart ثم إعادة بناء، ومراقب لموت المسار المحلي.
- **الناقص:** أربعة أعطال. (أ) `app_config.dart:39` قيمته الافتراضية `''` ولا
  `turn.env` ولا coturn في أي مكان ⇒ STUN فقط. (ب) `webrtc_audio_service.dart:532`
  سطر الكتم *معلَّق*، و`socket.service.ts:2095` يضيف كل عضو للصوت ⇒ 30 مايك مفتوح.
  (ج) `app_config.dart:62` خمس محاولات ثم لا معالج `reconnect_failed` ولا إعادة
  اتصال. (د) `room_screen.dart:2096` زر الضوضاء `setState` فقط.
- **أقل تغيير:** نصب coturn على سيرفر AWS وتمرير `TURN_URLS` من `build-release.sh`؛
  فتح الصوت `listenOnly` وتشغيل البث عند أخذ المايك فقط، وقصر `user_joined_voice`
  على الجالسين؛ `setReconnectionAttempts(infinity)` + إعادة اتصال عند العودة للمقدّمة.

**A17 — الحظر (×4)**
- **الموجود:** السيرفر صحيح تمامًا: مدد منفصلة للأدمن والسوبر، حصانة للطاقم،
  وإغلاق الغرفة للسوبر فقط.
- **الناقص:** `user.g.dart:57` لا يكتب `isSuperAdmin` في الكاش، و`auth_provider`
  يقرأ من الكاش عند الإقلاع ⇒ السوبر أدمن يصير أدمن عاديًا بعد أول إعادة تشغيل.
- **أقل تغيير:** إضافة `isSuperAdmin` و`displayId` إلى `_$UserToJson` وإعادة توليد
  الملف، أو استدعاء `fetchCurrentUser()` في `checkAuthStatus` بدل الكاش.

**D4 — حذف المنتج (×3)**
- **الموجود:** حذف صحيح آمن للعلاقات في `adminProduct.controller.ts:367` ومسجّل.
- **الناقص:** `app.js:1474` ينادي المسار الآخر، والحذف المباشر يفشل بـ 500 لأن
  `user_items.itemId` بلا cascade.
- **أقل تغيير:** تغيير الرابط إلى `/admin-products/products/` — سطر واحد.

**G3(c) — المالك لا يكسب ولا يخسر**
- **الموجود:** 12 لعبة تعمل، وسبع منها بـRTP مقيس ومحاكيات.
- **الناقص:** لا لعبة صفرية المجموع. المالك طرف مقابل في كل رهان؛ ألعاب المهارة
  تأخذ 45–60% وثلاثة ملفات توثّق ذلك كقرار مقصود.
- **أقل تغيير:** لا يوجد «أقل تغيير» — هذا إعادة تصميم اقتصادي: تحويل كل لعبة إلى
  وعاء لكل جولة (الرهانات تدخل، الفائزون يقتسمون، rake صفري) بدل التسوية المصرفية.

**G4 — صور المتجر**
- **الموجود:** `home_screen.dart:765` قائمة مكتوبة يدويًا بصورة واحدة مكررة ثلاثًا.
- **الناقص:** الصور الأربع نفسها.
- **أقل تغيير:** إضافة الملفات ثم استبدال القائمة — والأفضل جلبها من endpoint
  ليتمكن العميل من تبديلها من اللوحة بلا بناء جديد.

### أولوية عالية

**A18** — توسيع `socket.service.ts:321` إلى `OR: [{isSuperAdmin:true},{isAdmin:true}]`
ثم فصل كتلة `if (isAdmin)` في `room_screen.dart:5740` بحيث تُفتح شرائح المايك الأربع
لأدمن المنصة ويبقى الطرد والتعيين للغرفة.

**F4** — البنية كاملة؛ الناقص إرسال `x-device-id` من `dio_client.dart` وحفظ
`lastDeviceId`/`lastIp` على صف المستخدم وعرضهما في اللوحة.

**E1** — `xp.service.ts:46` يستخدم صيغة احتياطية *لكل مستوى على حدة*، فجدول ناقص
يقفز بالمستخدم إلى مستوى 100 بهدية واحدة. الحل: قصّ أي مستوى غير مضبوط على أعلى
عتبة مضبوطة تحته، وزر «حفظ الكل» في اللوحة.

**E3** — `vip.routes.ts:97` يكرر عطل اللوحة نفسه عند *شراء* VIP. استخراج منطق
الأرضية من `adminDashboard.controller.ts:231` إلى دالة مشتركة ومناداتها من الموضعين.

**B11** — نقل حساب الرصيد المتاح *داخل* المعاملة عند `Serializable` وحماية الخصم
بـ`updateMany` مشروط — هذا منع لسحب مزدوج للتارجيت يتحول إلى كوينز حقيقية.

### أولوية متوسطة

D6/D7/D8 كلها جذر واحد: `main.dart:399` يضع `GlobalNotificationBar` فوق كل شيء —
إعادة ترتيب واحدة تغلق ثلاثة بنود. D9 يحتاج فصل الودجت إلى شريطين.
C2/C3/C12/C16 كلها في كتلة هوية واحدة في `profile_screen.dart:1190`.
C11 يحتاج backfill بعد `adminUpsertVipLevel` وتبديل مصدر الشارات في الغرفة من
الإنجازات إلى `UserItem`. A15/A16/A20/B4/B5/B6/B7/D1/D3/D5/F2/G1/G2 تفاصيلها في §2.

---

## 4. الديون التقنية

1. **كود مالي خارج المعاملات.** `agencyReward.service.ts:44` ثلاث عمليات متتابعة بلا
   `$transaction` ⇒ شحنتان متزامنتان قد تدفعان نفس الدرجة مرتين.
   `adminDashboard.controller.ts:1318` يضيف رصيدًا ثم ينادي العدّاد خارج أي معاملة.
2. **TOCTOU في التارجيت.** `agency.controller.ts:1716` و`:1845` يحسبان المتاح خارج
   المعاملة — سحب مزدوج يتحول إلى كوينز بنسبة 50%. هذا هو عرض «أرقام وهمية معلقة».
3. **ثلاثة مفاهيم لرصيد الوكالة تتعايش:** `User.coinsBalance` الحقيقي،
   `ChargingAgency.balanceCoins` وعاء متقاعد ما زال قابلًا للكتابة، و`earnedCoins`
   عمود ميت لا يكتبه أحد.
4. **مساران للشحن باقتصاد مختلف.** `/agencies/send-coins` ينزّل تارجيت ويكتب
   `Transaction`؛ `/charging-agencies/transfer` لا يفعل أيًّا منهما.
5. **كود ميت:** `profile_screen.dart:2067` `_buildVIPCard`، `seats_grid.dart:27`
   `scrollable`، `sender_banner_layer.dart` بالكامل، `room_screen.dart:6566`
   `_openGiftSheet` (وفيه فخ: `add()` بلا `seatNumber`).
6. **61 ميجابايت أصول** غير مضغوطة، وAPK موحّد بلا `--split-per-abi`، ومتجر بلا
   ترقيم صفحات (`store.routes.ts:457`) — يناقض مباشرة مطلب «استهلاك بيانات قليل».
7. **`GLOBAL_DEFAULT_BLOCKED = true`** (`targetLock.ts:21`) — جدول إعدادات فارغ يحظر
   الجميع، وهو أرجح تفسير لبلاغ «اختفى البيع والتبديل».

---

## 5. محتاج تأكيد يدوي

| البند | لماذا | كيف تتأكد |
|---|---|---|
| F5 اللوحة الجديدة | لا يمكن معرفة أي كوميت يشغّله السيرفر | افتح `http://63.179.163.62:3000/admin-dashboard.html` وابحث عن أقسام: المشرفون / الألعاب / الرسائل والحظر. غيابها = السيرفر على بناء قديم. **الجواب للعميل: نفس الرابط ونفس البيانات.** |
| G2 موقع التحميل | نطاق خارجي | افتح `https://almobarmg.com/samafox` وتأكد أن النسخة المقدَّمة `1.0.18+24` |
| G6 حالة Play | حقول Console | Testing → Open testing (هل يوجد إصدار نشط؟) و Release → App bundle explorer (هل رُفع `24`؟) و Policy status |
| G5 ترتيب البحث | يعتمد على فهرسة Play | طبّق §2.2–2.4 من `docs/aso-plan.md` ثم أعد الفحص بعد ~7 أيام |
| A4 دائرة المتكلم | `audioLevel` يعتمد على الجهاز | جهازان حقيقيان في غرفة واحدة |
| A11 صوت التسجيل | سياسة التقاط متزامن في أندرويد | سجّل داخل غرفة فيها صوت على جهاز حقيقي |
| D1 هدايا الفيديو | `ffprobe` على سيرفر ARM | ارفع هدية فيديو؛ الفشل يعني `ffprobe` غير موجود على `PATH` |

---

## 6. فيتشرز في الكود بدون طلب مكتوب

- **لعبتان كاملتان:** أستيريون وبوابات أوليمبوس (6×5، عدالة مُثبتة، محاكي RTP).
- **نظام عدالة مُثبتة** لخمس ألعاب (`fairSeeds.ts`) — بذرة سيرفر مُلتزَم بها مسبقًا.
- **بوابة ألعاب** `gameConfig.service.ts` بتشغيل/إيقاف وحدود رهان لكل لعبة.
- **مسجّل شاشة داخل التطبيق** بخدمة أمامية أندرويد أصلية.
- **`build-release.sh`** يرفض بناء إصدار بلا TURN — حارس ضد العطل نفسه.
- **خطة ASO** كاملة في `docs/aso-plan.md`.

---

## 7. خطة التنفيذ

### الموجة 1 — عاجل (قبل أي نشر) · ~3–5 أيام
1. **A2 الصوت** — نصب coturn + إغلاق المايك المفتوح + إصلاح إعادة الاتصال. *(2–3 أيام، وحده أكبر من الباقي مجتمعًا)*
2. **A17** — سطر في `user.g.dart` + إعادة توليد. *(ساعة)*
3. **D4** — سطر واحد في `app.js`. *(10 دقائق)*
4. **B11** — نقل الحساب داخل المعاملة. *(نصف يوم)*
5. **E1/E3** — قصّ العتبات + دالة أرضية مشتركة. *(نصف يوم)*
6. **نشر `main`** على الإنتاج — بدونه لا شيء مما سبق يصل العميل. *(ساعة)*

### الموجة 2 — مهم · ~4–6 أيام
A18 صلاحيات المايك · F4 إرسال معرّف الجهاز · D6/D7/D8 إعادة ترتيب الطبقات ·
C2/C3/C12/C16 كتلة هوية البروفايل · C11 شارات الغرفة والـbackfill · A15 مكان زر
المكافأة · A16 حذف الكارت الميت وإنشاء غرفة 100000 · A20 مصدر بيانات الكارت ·
G4 صور المتجر · G2 رفع APK وربط النطاق.

### الموجة 3 — تحسينات · ~5–8 أيام + قرار تجاري
**G3(c) إعادة التصميم الاقتصادي** — هذا ليس تطويرًا فحسب بل قرار: الشكل المطلوب
(لاعبون يكسبون من بعض والمالك محايد) يعني تحويل كل لعبة إلى وعاء جماعي.
**قبل النشر:** ألعاب المهارة التي تُرجع 40–55% من الرهان هي بالضبط ما يستدعي مراجعة
سياسة القمار الحقيقي في Google Play — والعميل ضغط «نشر» في 30/08.
بعدها: ضغط الأصول 61MB→~18MB · `--split-per-abi` · ترقيم صفحات المتجر · تنظيف
الكود الميت والمفاهيم الثلاثة لرصيد الوكالة.

---

## 8. ملاحظة على الدمج

أثناء هذا التدقيق دُمج `audit-remaining-items` في `main` (`6cf571f`). الدمج لم يكن
نظيفًا: **10 ملفات متعارضة**، وأخطرها `games_hub_screen.dart` الذي أنتج تلقائيًا
خمس `case` مكرّرة — خطأ ترجمة يمنع البناء. `main` كان يُخفي أربع ألعاب خلف تعليقات
بينما الفرع يعيدها. حُلّ لصالح إظهار الاثنتي عشرة.

بعد الحل: 12 عنوانًا ↔ 12 `case`، و`flutter analyze` = **صفر أخطاء وصفر تحذيرات**،
وكل ملفات المصدر مطابقة حرفيًا لنسخة الفرع التي بُني منها APK بنجاح.

`main` الآن **40 كوميت فوق `origin/main`** ولم يُدفع بعد — الدفع والنشر قراركما.

---

## 9. اختبار حيّ في المتصفح (12/09)

شُغّل التطبيق فعليًا: باك إند محلي + PostgreSQL + بناء Flutter web، وسُجّل دخول
بحساب سوبر أدمن حقيقي. ما يلي **مُشاهَد**، لا مقروء من الكود.

**ملاحظة على البيئة:** أول محاولة لإنشاء غرفة فشلت بـ 500 لأن قاعدة الاختبار
أُنشئت بترميز WIN1252 فلم تقبل العربية. **هذا عيب في بيئتي لا في التطبيق** —
أُعيد إنشاؤها بـ UTF8 وعملت. تأكّد فقط أن سيرفر الإنتاج UTF8.

### أُثبت أنه يعمل
- تسجيل الدخول، الواجهة عربية بالكامل، واتجاه RTL سليم.
- إنشاء غرفة بـ 30 مايك، ودخولها، وظهورها في القائمة.
- **A12** التمرير الأفقي لاسم الغرفة الطويل يعمل فعلًا (شوهد وهو يتحرك).
- أيقونة الكأس في هيدر الغرفة.

### أُثبت أنه مكسور
- **G4** — `Ads.png` ليست إعلانًا بل **صورة تصميم (mockup)** فيها ثلاثة إعلانات
  وهمية بجوار بعضها، ومعها نقاط كاروسيل وأزرار **مرسومة داخل الصورة**. التطبيق
  يعرض هذا الـmockup ثلاث مرات، فتظهر نقاطه المرسومة بجانب نقاط الكاروسيل
  الحقيقية. والإعلانات نفسها عن ألعاب فانتازيا لا علاقة لها بتطبيق دردشة صوتية.
- **A16** — كارت «غرفة سما فوكس / ADMIN ROOM» القديم **ما زال معروضًا** بحجم
  كبير أعلى القائمة، تمامًا كما يقول `home_screen.dart:445`.
- **A5** — عند 30 مايك الشبكة **مقصوصة**: يظهر أربعة صفوف والصف الخامس
  (المايكات 25–30) غير مرئي، وأرقام الصف الرابع مقطوعة.

### عيب جديد خارج قائمة العميل
**حقل «رقم الغرفة (اختياري)» بلا أثر.** الواجهة ترسله
(`create_room_dialog.dart:98`) والباك إند يفكّكه (`room.controller.ts:254`)
لكن موديل `Room` **لا يحتوي على العمود أصلًا** — أُثبت بخطأ Prisma
`Unknown field roomNumber` وبفحص الصف بعد الإنشاء. القيمة تُبتلع بصمت.
وما يظهر في هيدر الغرفة تحت مسمى `ID:` هو **الأيدي الظاهر لصاحب الغرفة**
(`room_screen.dart:4235` `'ID: $ownerDid'`) لا رقم الغرفة — وهو ما يضلّل أي
مستخدم يحاول مشاركة «رقم الغرفة».

### A17 — نتيجة غير حاسمة، وهذا مقصود
قراءة `localStorage` مباشرة أثبتت أن النسخة المخزَّنة من المستخدم تحتوي 26 حقلًا
بالضبط — نفس ما يكتبه `_$UserToJson` — و**`isSuperAdmin` غير موجود فيها**. فالفقد
حقيقي ومؤكَّد.

لكن بعد إعادة تحميل كاملة ظهر «إغلاق الغرفة» رغم ذلك، وهو خيار محجوب خلف
`if (!isSuper) return;` (`room_screen.dart:1348`). لم أستطع التوفيق بين الأمرين
في هذه الجلسة، فلا أدّعي أن «كل إقلاع يُنزّل السوبر أدمن» — هذا **لم يتأكد**.
المؤكَّد أن الكاش يفقد الحقل، وأن السلوك الصحيح يعتمد على إعادة جلب لاحقة قد
تحدث أو لا تحدث. الإصلاح (إضافة الحقل إلى `toJson`) يزيل الغموض كليًا وتكلفته سطر.

### لم يُختبر
- **A2 الصوت** — يحتاج جهازين على شبكتين خلف CGNAT؛ متصفحان على نفس الجهاز لا
  يثبتان شيئًا عنه.
- **الهدايا وCP والتارجيت** — قاعدة نظيفة بلا هدايا مزروعة.
- **مستخدمان معًا** — التبويبان يتشاركان `localStorage` فلا يمكن جلستان في متصفح واحد.


---

## §10 — ما أُغلق بعد التدقيق (12/09/2026)

التدقيق أعلاه للقراءة فقط. ما يلي سجل التنفيذ الذي طلبه العميل بعده: البنود
الحمراء الخمسة ثم البنود الصفراء الخمسة والثلاثون. `main` الآن على `ca5a673`.

**أُغلق (31 بندًا):**

| المجموعة | البنود |
|---|---|
| الصوت | A1 · A3 · A5 · A11 · A15 · A16 · A17 · A18 · A20 |
| الوكالات | B4 · B5 · B6 · B7 · B11 |
| المظهر | C2 · C3 · C10 · C11 · C12 · C16 |
| الهدايا | D1 · D3 · D4 · D5 · D6 · D8 · D9 |
| الليفل/VIP | E1 · E3 |
| اللوحة | F2 · F4 |
| الأداء | G1 (الشق القابل للإصلاح) |

**تغييرات تحتاج انتباهًا عند النشر:**

1. **هجرتان جديدتان** — `20260912000000_topup_requester` و
   `20260912010000_user_last_device`. شغّل `prisma migrate deploy` قبل تشغيل
   الخادم الجديد، وإلا فشل طلب شحن الفرع وتسجيل الجهاز.
2. **B4 يغيّر وجهة زر موجود** — `PATCH /charging-agencies/:id/balance` صار يكتب
   في محفظة الوكيل بدل `ChargingAgency.balanceCoins`. هذا هو الإصلاح نفسه، لكن
   أي إجراء إداري سابق اعتمد على الرصيد القديم لن ينعكس تلقائيًا.
3. **C11 يُنفّذ backfill** — حفظ مكافآت أي فئة VIP يمنحها الآن لكل من هو عند تلك
   الفئة أو فوقها. على فئة كبيرة قد يستغرق ذلك دقائق في الخلفية (آمن التكرار).
4. **D9 تفسير لطلبين متناقضين** — D6 طلب الشريط يمين، D9 طلبه في النص، وهما على
   ودجت واحدة. الفصل الآن بحسب من ينظر: المُرسِل يراه يمينًا، وبقية الغرفة في
   النص. إن لم يكن هذا المقصود فالتعديل سطر واحد.

**لم يُغلق (4 بنود + G3):**

| البند | السبب | من يملك القرار |
|---|---|---|
| A2 (الجزء الأخير) | يحتاج خادم TURN حقيقي — بنية تحتية لا كود. سكربت البناء يرفض البناء بدونه بالفعل | العميل/الاستضافة |
| G2 | رفع APK وضبط DNS لـ `almobarmg.com` | العميل |
| G4 (الصور الأربع) | الملفات غير موجودة في المستودع إطلاقًا | العميل |
| G5 | حقول Play Console | العميل |
| G3 | «صفر عمولة» على 12 لعبة ممولة من البيت. الألعاب الجولية يمكن جعلها pari-mutuel؛ سلوت اللاعب الواحد لا خصم له، فـ«صفر» هناك تعني RTP 100% أي خسارة مؤكدة للبيت. قرار بنيوي قبل أي كود | العميل |

**G1 — حدود ما أمكن:** الشق الأصلي (APK موحّد لثلاث معماريات) أُغلق. الـ61MB
المتبقية أصول فنية: R8 وتقليص الموارد لا تمسّها، وإعادة الضغط بلا فقد على كل
الـ135 صورة توفّر أقل من 4%، والملفات التي تبدو غير مستخدَمة تُحمَّل بمسارات
مُركَّبة فحذفها يكسر الألعاب. تقليصها أكثر = إعادة ترميز الفن، وهو قرار عن شكل
المنتج لا عن إعداد بناء.

**التحقق:** `tsc --noEmit` نظيف · `flutter analyze` صفر أخطاء وتحذيرات ·
`flutter test` 37/37 (حُذف قالب العدّاد الافتراضي الذي كان فاشلًا منذ أول commit).
