<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ADD THESE LINES -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- No READ_MEDIA_IMAGES / READ_MEDIA_VIDEO: photos come through the system
         photo picker (image_picker), which needs no permission, and Play's Photo
         and Video Permissions policy rejects the broad permission on API 33+.
         READ_MEDIA_AUDIO stays — the photo picker cannot pick songs. -->
    <!-- Android 13+: needed to pick songs off the phone for room music -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>

    <!--
      A23 - "الاحتفاظ بالغرفة": audio must keep running while the app is in the
      background, "كأنه لم يخرج من الروم إطلاقاً". Android 9+ cuts microphone
      capture for a backgrounded process unless a foreground service of type
      `microphone` is running, so these are what make the reserved mic keep
      transmitting instead of going silent.

      PLAY POLICY: FOREGROUND_SERVICE_MICROPHONE needs a matching declaration
      and justification in Play Console. The same developer account already had
      an app (AiZone) rejected for "Play Console declaration and/or app
      description does not sufficiently demonstrate the use of the
      FOREGROUND_SERVICE permission", so fill in the declaration - "keeps the
      user's live voice chat running while they use other apps" - BEFORE
      submitting this build.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!--
      A11 - "تسجيل الشاشة بدون صوت". Screen capture must run inside a
      foreground service of type mediaProjection from Android 10, and Android 14
      additionally requires this dedicated permission.

      PLAY POLICY: like FOREGROUND_SERVICE_MICROPHONE above, this needs its own
      declaration in Play Console - "lets the user record their own session in a
      voice room" - before submitting.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <!-- Android 13+: the ongoing "mic is open" notification needs consent. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- G5 — the label was the literal "samafox": lowercase, English, and not
         what any of this app's users would search for. It is the localised
         @string/app_name now, which resolves to "سما فوكس" on an Arabic
         device via res/values-ar/strings.xml. -->
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher">

        <!-- Facebook SDK -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"/>

        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="42eb5bd991176d588ca56381a3ccff20" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />

        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!--
          A23 - holds the process in a state where Android keeps letting it
          capture the microphone while backgrounded. It owns no audio itself;
          WebRTC still does. `exported=false` because nothing outside the app
          has any business starting it.
        -->
        <service
            android:name=".RoomAudioService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!--
          A11 - owns the MediaProjection capture. `mediaProjection` is the type
          Android requires for screen recording; `microphone` is declared too
          because the recorder captures the mic (the room's own voice stream is
          USAGE_VOICE_COMMUNICATION, which the platform forbids any app from
          capturing - see ScreenRecordService).
        -->
        <service
            android:name=".ScreenRecordService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|microphone" />

        <!-- image_cropper's native crop UI (profile photo crop tool) -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <!-- Render with Skia, not Impeller. On Android 13+ phones (realme
             RMX3760, Samsung A53/A54, Xiaomi) the app sat at 700MB within
             seconds of entering a room and climbed to 1-1.6GB until the OS
             killed it; Android 10-12 phones in the same rooms stayed at
             200-500MB. Impeller's Vulkan allocations are mapped into the
             process and counted against it, Skia's are not. -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
